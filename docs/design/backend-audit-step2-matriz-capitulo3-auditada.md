# Matriz para capítulo 3 — auditada (Paso 2)

**Base:** `docs/design/backend-chapter3-preliminary-matrix-2026-09-24.md` (no modificada; este documento es adicional).
**Snapshot auditado:** `f17fe658bad4726202fe13784bb716c06468af9a`.
**Categorías de estado:** Respaldada en el alcance comprobado / Requiere redacción limitada / Pendiente de corrección / No comprobada.

Referencias `[F-xx]`/`[D-xx]` apuntan a `informe-auditoria-paso2.md`.

| Capacidad (matriz preliminar) | Estado auditado | Justificación |
| --- | --- | --- |
| Pronóstico operativo legacy por sensor | **Respaldada en el alcance comprobado** | F-01 PASS por lectura de código + 13 tests ejecutados (`test_forecast.py`, `test_pipeline.py`). La precedencia recalibrado→caché→RF y la inmutabilidad de emisión se confirmaron. |
| Ingeniería temporal operativa | **Requiere redacción limitada** | F-02 PASS solo para los casos de fixture ejecutados. La spec describe "split crudo antes de imputar por partición" pero el código ejecuta una única pasada de `ffill` sobre la serie completa; son equivalentes matemáticamente (`ffill` es estrictamente causal), pero la memoria no debería afirmar literalmente "se parte antes de imputar" sin la aclaración de equivalencia. Redactar como "orden funcionalmente equivalente al split-antes-de-imputar, verificado con fixture dirigido; no probado para toda configuración". |
| Emisión operativa v2 multihorizonte | **Respaldada en el alcance comprobado, con precisión obligatoria** | F-03 PASS: el calibrador (`CalibratedClassifierCV(FrozenEstimator(model), ...)`) encapsula al modelo entrenado; usarlo directamente es correcto, no una omisión. Aclarar en la memoria que `model.joblib` se conserva por identidad/trazabilidad, no porque se invoque en cada inferencia. No hay bundles reales para validar operabilidad (ver fila de inventario). |
| Persistencia operacional v2 | **Respaldada en el alcance comprobado** | F-04 PASS: lock interproceso real a nivel de SO (`msvcrt.locking`/`fcntl.flock`), no solo reemplazo atómico de archivo, más idempotencia por clave con detección de conflicto de contenido. Verificado por lectura de código + tests existentes (no se forzó una carrera multiproceso propia para este ítem). |
| Catálogo y lecturas por sensor | **No comprobada en este paso** | Fuera del foco de Prioridad 1 de este encargo; no se leyó `CatalogRepository`/`query_readings` en profundidad. Mantener la formulación preliminar como pendiente de un paso posterior. |
| Evaluación experimental v4 | **Respaldada en el alcance comprobado** | F-12 PASS: 79 tests ejecutados sobre `models`, `soft_voting`, `selection`, `artifacts`, `transfer_contract`, `config` — todos passed. No se abrió holdout ni se interpretaron resultados de campaña. |
| Ensamble soft voting | **Respaldada en el alcance comprobado, con matiz de empate** | F-12: pesos 1/3 confirmados en código; `sample_weight` fold-local confirmado (cada base calcula el suyo dentro de `fit`, sin propagación global). Empate exacto en 0.5 resuelve a clase 0 vía `argmax` (comportamiento estándar, sin consumidor `>=` divergente identificado). La matriz preliminar ya distingue correctamente "pesos 1/3 no son sample_weight" — mantener esa distinción explícita en la redacción final. |
| Contratos y artefactos A→B→C | **No comprobada en este paso** | No se ejecutaron los tests de `stage_b/stage_c/holdout_ledger/admissibility` en esta auditoría (fuera del subconjunto priorizado F-01..F-12 con foco en backend/replay/feedback). Mantener como pendiente. |
| Reproducción histórica causal | **Pendiente de corrección parcial (BLOCKED explícito, causa confirmada)** | F-06/F-07 PASS solo por tests unitarios y fixtures sintéticas propias (no el paquete real). La validación HTTP end-to-end contra el paquete real está formalmente **BLOCKED**: causa raíz confirmada (no hipotética) es un artefacto de checkout (`core.autocrlf=true` en la máquina de auditoría rompe el hash de custodia de los archivos de texto del paquete: `IntegrityError` en `_verify_file_integrity`), ajeno al contenido del paquete (el blob de git coincide con el manifiesto) y ajeno a versiones de librerías. La memoria NO debe afirmar que el contrato HTTP fue revalidado end-to-end contra el paquete real en este paso; sí puede afirmar que la lógica de proyección fue verificada con fixtures y tests unitarios. La cifra "20/18/124 aprobadas" [D-05] sigue sin SHA ejecutable documentado en la fuente; se reprodujo 124 passed sobre `f17fe658...` de forma independiente (evidencia circunstancial). |
| Deriva contractual del replay | **Pendiente de corrección (reclasificado: NO está resuelta)** | D-06 se reexaminó: hay una obligación **MUST** vigente en la spec delta del change (`RH-05`, con un Scenario explícito sobre "historial mostrado antes de la revelación"; `RH-08`) que `GET /replay/history` no satisface (`ReplayHistoryRow` no tiene ningún campo de estado). `traceability.md` marca RH-05/RH-08 como "[implementada]" pese a admitir la brecha en su propio texto — esto es una contradicción documental real, no una casilla de `tasks.md` válidamente diferida. La memoria NO debe describir esto como "contrato deliberadamente acotado y documentado"; debe describirlo como una obligación de spec pendiente de resolución normativa (corregir código o acotar spec — decisión no tomada aún). |
| Vinculación predicción–medición en replay | **Pendiente de corrección (mismo gap que D-06, reclasificado)** | Cubierto por F-07 en su parte de fixtures; `imputation_markers_df=None` y ausencia de estado por fila en `/history` ya no se consideran un diferimiento válido (ver D-06 reclasificado) sino la misma obligación MUST pendiente. |
| Feedback legacy | **Pendiente de corrección (nuevo hallazgo confirmado)** | F-09: la madurez/procedencia sigue **PASS** (tests existentes). Pero la concurrencia, que en el informe anterior figuraba como "no comprobada", se comprobó con una fixture dirigida (dos procesos, `load→update→save`, sincronizados con barrera) y **se confirmó una pérdida de actualización ("lost update")**: una de dos validaciones humanas concurrentes desaparece silenciosamente sin ningún error. Distinto mecánicamente de F-08 (sin corrupción de bytes; pérdida semántica por ausencia de lock en el ciclo completo). La memoria debe describir esto como un defecto conocido, no como una limitación de cobertura de pruebas. El caso `target_timestamp=NaT` sigue sin fixture propia (limitación real, no reforzada ni debilitada). |
| Recalibración manual supervisada | **Respaldada en el alcance comprobado** | F-10 PASS: tests dedicados ejecutados y pasando (`test_recalibration_is_not_reapplied_and_old_test_is_not_reused`, cadena A→B→C, no-registro ante fallo/no-op). No se afirma mejora de desempeño (correcto, mantener). |
| Linaje de recalibración | **Pendiente de corrección (documental)** | D-01 CONFIRMADA: la spec vigente (`human-feedback/spec.md` línea 231, "T-01") declara que no existe endpoint HTTP de linaje, pero `GET /lineage/{sensor_id}` está implementado y testeado desde el commit `7cdea8b`. La memoria no debe repetir la limitación T-01 tal como está redactada en la spec hoy; debe describir el endpoint como existente, citando la corrección pendiente en la spec. |
| Reviews operativas v2 | **Requiere redacción limitada** | F-11: confirmado por traza estática que no existe ningún consumidor de `training_eligibility`; "registrar una review no la dispara" (recalibración) es correcto tal como está redactado en la matriz preliminar. Agregar que no se encontró ninguna obligación normativa vigente que exija ese puente (no es una carencia respecto de un contrato existente). |
| Feedback de reproducción | **Pendiente de corrección** | F-08 CONFIRMÓ (no solo "sin prueba", como decía la matriz preliminar) una **pérdida y corrupción de datos reproducible** bajo escritura concurrente real (411/1600 líneas perdidas, 1 línea corrupta, fixture con barrera multiproceso). La memoria no debe describir esto como limitación de cobertura de pruebas únicamente; debe describirlo como un defecto conocido con corrección mínima pendiente (ver encargo). |
| Integración backend–UI | **Respaldada en el alcance comprobado** | D-02: traza estática confirma ausencia de import/adapter de `controlled_daily_v4` en rutas operativas backend. Sin contradicción documental detectada. |
