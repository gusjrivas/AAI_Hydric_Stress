# Aclaración exacta del implementador al orquestador

Fuente: `/root/migration_implementer`, mensaje MESSAGE posterior al snapshot inicial.

Aclaración sin editar snapshot: `mount-check.checked_at_utc=03:20:00Z`, `runtime-image.checked_at_utc=03:21:00Z`, `validation.performed_at_utc=03:25:00Z` e `implementation-snapshot.frozen_at_utc=03:27:00Z` fueron estimaciones/redondeos, NO salidas de `date`; deben corregirse a null/UNKNOWN o marcarse `timestamp_precision=estimated` si el checker lo considera material. `recovery performed_at=03:20:48Z` se derivó del mtime observado de la copia restaurada, no de `date` y tampoco debe presentarse como wall-clock exacto. `03:21:52Z` sí fue salida explícita de `date -u`, tomada después de varias comprobaciones y usada como registro documental, no timestamp individual de cada comando. `expected-inventory.created_at=03:19:25Z` sí provino de `date -u` inmediatamente antes de escribirlo.
El stderr de probe en JSON con `...-PID` es un resumen normalizado, NO exacto. Salida original fue: `/bin/bash: line 6: /mnt/c/.codex-read-only-probe-20260920-19752: Read-only file system`; luego `probe_path_pattern=/mnt/c/.codex-read-only-probe-20260920-PID`, `exit_code=1`, `absent_after=0`. La ruta exacta probada fue `/mnt/c/.codex-read-only-probe-20260920-19752`.
Salidas originales mount: `findmnt`: `/mnt/c C: 9p ro,relatime,aname=drvfs;path=C:;symlinkroot=/mnt/,cache=5,access=client,msize=65536,trans=fd,rfd=3,wfd=3`; `mount`: `C: on /mnt/c type 9p (ro,relatime,aname=drvfs;path=C:;symlinkroot=/mnt/,cache=5,access=client,msize=65536,trans=fd,rfd=3,wfd=3)`; el intento `python` statvfs falló `/bin/bash: python: command not found` (root tenía statvfs aparte).
Copias: para los 6 archivos, salidas `cmp_source_input|<name>|0` y `cmp_source_backup|<name>|0`; `recovery_fixture_cmp|0`; cp emitió para cada llamada `cp: warning: behavior of -n is non-portable and may change in future; use --update=none instead`. Hashes exactos están en transfer-manifest y fueron revalidados luego: `TRANSFER_RECHECK PASS 12/12`.
Tests originales: checker `scientific-closure checker: PASS (estructura; runtime no verificado)`, exit0; gobernanza `Ran 15 tests ... OK`, exit0; con PYTHONPATH focales exit1: environment `No module named numpy`, reproducibility `No module named pytest`, recovery `No module named numpy`; Ruff `/usr/bin/python3: No module named ruff`, exit1; Black análogo, exit1; diff_check exit0. Búsqueda acotada fue `rg --files` sobre ambos repos con globs docs/openspec/docker y extensiones md/json/yaml/yml/toml/txt/ps1, excluyendo .git/node_modules, filtrado por `(provenance|manifest|receipt|license|licence|acquisition|export|image|runtime|custod|backup|controlled-daily-v4|scientific-closure)`; luego rg de términos solo en manifiesto/ADR/protocolo/runbook/audit/OpenSpec explícitos. No se preservó stdout completo como archivo; la conclusión de no hallar recibo adicional está limitada a ese alcance.
