# Registro de decisiones de gobernanza

Todas son decisiones de preparación, no resultados científicos. Decisiones
GD-01..10: 2026-09-18; GD-11..12: 2026-09-19; GD-13..18: 2026-09-20;
GD-19..23: 2026-09-20, sesión de verificación de cierre.

| ID | Decisión y fundamento | Impacto / autoridad |
| --- | --- | --- |
| GD-01 | Extender OpenSpec existente (project/specs/changes); agregar config spec-driven y capacidad scientific-closure | Encargo actual. No init destructivo ni migración de changes históricos |
| GD-02 | R/H/N/S condicionales por CL-05..08 y revisión de suficiencia global | Encargo actual prevalece sobre obligatoriedad indiscriminada en auditoría anterior; diseños congelados preservados |
| GD-03 | Separar PASS auditor, gate de candidato y cierre de tesis | Un resultado negativo puede cumplir protocolo y recibir PASS; no abre etapa bloqueada |
| GD-04 | Mantener SHA ejecutable auditado separado de SHA documental | Auditoría previa identifica 214735e42ee04f018156cd630591e798aadd8bf3 e imagen 55bc923efac0; toda nueva campaña debe verificar identidad completa |
| GD-05 | No modificar main para resolver ADR-0011 | Último cambio de protocolo (214735e) no es ancestro de main local (comprobado exit 1). La vigencia de la precondición ante correcciones posteriores queda por resolver por responsable; bloquea ejecución, no especificación |
| GD-06 | Usar OpenSpec CLI 1.13.1 por npx fijado, sin instalación global ni reemplazar dependencias científicas | npm view consultado; no había CLI global ni config.yaml |
| GD-07 | TOML autónomos en .codex/agents, agents.enabled y max_concurrent_threads_per_session=4 | Documentación oficial consultada; límite excluye padre. Runtime actual ofrece 4 slots totales: usar como máximo 3 hijos aquí |
| GD-08 | Conservar modelo solicitado gpt-5.6 en archivos | Documentación identifica alias de gpt-5.6-sol. Herramienta de colaboración ofrece nombre canónico; si se usa, registrar resolución sin afirmar degradación |
| GD-09 | Bloqueo de sandbox no equivale a hallazgo científico | Primer intento del explorador bloqueado por setup refresh; la segunda lectura con escalación completó la inspección, como registra preparation-validation.md. No emitió veredicto final |
| GD-10 | Readiness del sistema no es autorización para campaña | La siguiente sesión puede resolver preparación; A/B/ledger/C necesitan autorización explícita vigente y gates |
| GD-11 | Sustituir la instrucción operativa GD-08: los perfiles incompatibles `gpt-5.6` se reemplazan por `gpt-5.6-sol`, conservando medium/high/xhigh; Terra/medium y Luna/low se mantienen | Prompt vigente y catálogo local observado; requested/effective permanecen separados y el modelo efectivo no observable queda `null`. No acredita sandbox ni disponibilidad de campaña |
| GD-12 | Fijar antes de ejecución la suficiencia delegada: R `NOT_REQUIRED` para clasificación P20; H `REQUIRED` solo para correcciones simuladas; N `NOT_REQUIRED` sin afirmar detección reservada/fallas reales; S `NOT_REQUIRED` para el límite histórico de etiquetas+ruido, sin sensores ausentes | Autoridad delegada del prompt vigente, exploración y advisory crítico. CL-10 admite terminal negativo A/B, soporte insuficiente/onset indefinido, v3 `REFERENCED`, H auditado y límites; no certifica humano real, HU1 ni tesis completa. Pendiente crítica/auditoría de este snapshot |
| GD-13 | Declarar `ADMISSIBLE_WITH_EXPLICIT_LIMITATIONS` la procedencia de los dos CSV de Pergamino. **Corrección 2026-09-20 (hallazgo C-08):** la redacción original decía que esto «cierra la condición 2 de ADR-0011». Eso excedía la evidencia. La condición 2 exige confirmar o descartar explícitamente URLs y licencias **en el manifiesto de provenance**, y `controlled-daily-v4-external-pergamino-manifest.yaml` conserva `PENDING_CONFIRMATION` en esos campos. El estado correcto, que el propio `provenance-and-licence-assessment.json` ya registraba, es `RESOLVED_IN_SUBSTANCE_BY_EXPLICIT_DECISION, NOT YET REFLECTED IN THE MANIFEST`: la decisión existe y está fundada, pero la condición 2 **no** está formalmente satisfecha. Actualizar el manifiesto es acción de quien integre este trabajo en `main`, porque el manifiesto forma parte de la identidad congelada verificada idéntica a `214735e` | Evidencia oficial recuperada el 2026-09-20: las páginas propias de NASA POWER publican exigencia de cita pero **ninguna** licencia ni restricción de uso (negativo verificado en dos páginas); la guía oficial de NASA ESDIS establece que, sin restricción marcada, los datos de ciencias de la Tierra son CC0 y «there are no restrictions on the use of these data». ERA5-Land vía Open-Meteo ya tenía términos CC-BY-4.0 verificados. Se conservan como límites: fecha de adquisición efectiva `UNKNOWN` (nunca derivada de mtimes), términos vigentes al instante de adquisición no verificados, `downloaded_service_version` de POWER `UNKNOWN`. Registro: `readiness-resolution-linux-2026-09-20/provenance-and-licence-assessment.json` |
| GD-14 | Fijar un entorno **reconstruido** (venv con `constraints.txt`, Python 3.11.16) como entorno de validación de esta sesión, sin atribuirle la identidad de la imagen histórica | Docker no es alcanzable desde esta distro (integración WSL deshabilitada; sockets de Docker Desktop deniegan permiso a uid 1000; `sudo` exige autenticación interactiva). El set de paquetes resultó idéntico al `pip-freeze.txt` histórico (23/23) y la suite v4 pasa 486/486. La imagen `sha256:55bc923e…b297af` **no** fue inspeccionada, exportada ni reconstruida, y no se afirma equivalencia. `expected-inventory.json` prohíbe «rebuild and claim equivalence»: esta decisión no reconstruye la imagen ni reclama equivalencia. Ninguna campaña A/B/C fue iniciada, de modo que no se rompe identidad intra-campaña |
| GD-15 | Corregir el identificador del CLI de OpenSpec: el paquete npm es `@fission-ai/openspec@1.13.1` | El nombre `openspec` a secas resuelve a un placeholder cuya única versión publicada es `0.0.0` (`npm view openspec versions`). `@fission-ai/openspec` tiene `latest = 1.13.1`, exactamente la versión que fija GD-06. Con esa corrección, los diez `sc-*` y `spec/scientific-closure` validan `--strict` 11/11. No cambia la versión normativa, solo hace reproducible el comando |
| GD-16 | Corregir el comando documentado de A/B/C a `python -m experiment_runner.controlled_daily_v4.cli` y **no** modificar `src/` | El comando del runbook (`python -m experiment_runner.controlled_daily_v4`) no es invocable: el paquete carece de `__main__.py`. Se corrige la documentación en lugar de agregar `__main__.py` para preservar una propiedad de evidencia verificada: `src/`, `docker/` y `pyproject.toml` en HEAD son **byte-idénticos** al commit ejecutable declarado `214735e…`. Queda registrado como defecto pendiente, para corregirse junto con la condición 4, el docstring obsoleto de `src/experiment_runner/controlled_daily_v4/__init__.py`, que aún afirma que B y C no están implementadas |
| GD-17 | Adoptar `bubblewrap` como enforcement técnico de solo lectura para los roles lectores | Resuelve el fondo de AUD-READ03 y LNX-03, que exigían aislamiento verificable y no mera conducta. **Corrección 2026-09-20 (hallazgo C-05):** la redacción original incluía también `CRIT-SUB-01`. No corresponde: `CRIT-SUB-01` y `AUD-SUB-02` exigen **carga efectiva y permisos de los cinco perfiles**, que es cosa distinta del enforcement de solo lectura y sigue sin acreditarse (`agent-capabilities.json` concede que los perfiles `.codex/agents` no son cargables en este runtime). `CRIT-SUB-01` permanece `OPEN/BLOCKED` y SC-GOV-006 se degrada a `BLOCKED`. `scripts/readonly_role_sandbox.sh` + `scripts/verify_readonly_sandbox.sh` + `tests/test_readonly_role_sandbox.py` (**11 pruebas** al 2026-09-20; la cifra original «8» corresponde a la primera versión y creció a 9, 10 y 11 al cerrarse los hallazgos N-01, A-01 y NF-01 — corrección del hallazgo C-11) acreditan: escritura al repositorio y a `$HOME` rechazada con EROFS, sin red, `/tmp` efímero, sin fugas. Límite conservado: el harness no coloca el proceso del subagente dentro del sandbox; el enforcement cubre los comandos de shell que el rol lector ejecuta a través del envoltorio |
| GD-18 | Fijar `/home/gus/scientific-closure-runtime/` como raíz runtime Linux (`env/`, `evidence/`, `ledger/`, `backups/`, `logs/`) | El README fija la raíz de evidencia fuera del checkout pero solo en notación Windows (`C:\Repo\…`), inexistente en este entorno. La raíz Linux queda declarada, vacía de evidencia científica: ledger **no inicializado**, A/B/C **no ejecutadas**, holdout **no abierto**. La segunda copia sigue siendo **lógica**: todas las rutas comparten el device 2128 (`/dev/sdf`); `/dev/sdd` (1 TiB, ext4) existe sin montar y montarlo exige root |
| GD-19 | Registrar como **incumplimiento comprobado, no subsanado**, los dos `git push` realizados durante la preparación del 2026-09-20 (`dc0d3f5` 03:52:42Z y `b9fefbf` 05:54:37Z), contra la cláusula «no push en preparación» del criterio de aceptación de SC-GOV-019 y contra `AGENTS.md` | Hallazgo C-02 de la crítica independiente, verificado por `git reflog show refs/remotes/origin/feat/scientific-closure` y `git ls-remote`. SC-GOV-019 se degrada a `BLOCKED`. Los dos informes de auditoría que afirman «no push» se conservan **sin alterar**, conforme a la regla de preservar informes exactos de lectores; la corrección vive en `closure-verification-2026-09-20/findings-resolution.json`. La instrucción de sesión del 2026-09-20 autoriza push a `origin/feat/scientific-closure` **hacia adelante** y no puede autorizar retroactivamente los anteriores. Ningún revisor lo detectó porque los tres corrieron sin red, aunque el reflog es local |
| GD-20 | Fijar como precondición de reproducibilidad de toda validación OpenSpec que el node Linux de `/home/gus/scientific-closure-runtime/env/node/bin` esté **primero en `PATH`** | Hallazgo E-01. Con el `PATH` de login por defecto `npx` resuelve a `/mnt/c/Program Files/nodejs/npx`, binario de Windows; CMD.EXE rechaza el directorio UNC de WSL, cae a `C:\Windows` y el CLI no lee ningún repositorio. Las once validaciones terminan 1 con «no deltas found», y un change inexistente produce el **mismo** mensaje y código, lo que prueba que no se lee nada. Con el node Linux en `PATH` el resultado registrado 11/11 exit 0 se reproduce exactamente, de modo que el registro anterior **no queda refutado**: le faltaba esta precondición. El binario preexistente fue verificado byte-idéntico a la release oficial `node-v22.20.0-linux-x64` (sha256 `b1cbec894e45a5814b6ab756e1e14f8a76516273197e67e0412b57c1e10d0d9f`), contra el `SHASUMS256.txt` oficial |
| GD-21 | Reformular el bloqueo de imagen: lo inalcanzable es el **runtime de contenedores desde esta distro WSL**, no la existencia de la imagen aprobada | Hallazgo C-18. `final-preparation-audit.md` registra que la imagen `sha256:55bc923e…b297af` **fue ejecutada** el 2026-09-19 desde el host Windows (15 tests, 15 OK, 0,552 s). Reverificado aquí que desde esta distro no hay acceso: `/var/run/docker.sock` ausente; los 33 sockets de Docker Desktop son `root:root` y `connect()` devuelve EACCES para uid 1000; el `docker` del `PATH` es el binario de Windows y declara que la distro no tiene integración; `sudo -n` exige autenticación interactiva; `podman`, `skopeo`, `umoci`, `buildah` y `nerdctl` ausentes y `apt-get` exige root; `newuidmap`/`newgidmap` **no existen**, de modo que tampoco es viable un runtime rootless. Consecuencia adicional: el runbook ejecuta cada comando A/B/C a través de `docker`, así que este bloqueo impide **ejecutar la campaña**, no sólo inspeccionar la imagen. Se resuelve con acción del responsable sobre el host, no con más trabajo aquí |
| GD-22 | No reconstruir una imagen en esta sesión, pese a estar autorizado | La instrucción permite reconstruir desde commit, constraints y Dockerfile congelados y registrarla con digest nuevo sin hacerla pasar por la histórica. No se hizo porque no desbloquea nada: no hay runtime para construirla ni para ejecutarla, el runbook exige `docker` para A/B/C y la condición 4 de ADR-0011 bloquea la ejecución con independencia de la imagen. Construir un artefacto inutilizable habría añadido trabajo sin cambiar ningún estado. Queda registrado como decisión, no como omisión |
| GD-23 | Degradar SC-GOV-021, SC-GOV-023 y SC-GOV-024 de `NOT_APPLICABLE` a `BLOCKED` | Hallazgo C-09. `NOT_APPLICABLE` es **terminal** para el checker, tan definitivo como `PASS`, y su única base era la decisión de suficiencia GD-12, que `claims.md` declara «pendiente de crítica y auditoría independiente». Un estado terminal sobre una decisión autodeclarada no auditada contradice la regla de cerrar sólo con PASS de auditor. Además los artefactos `auxiliary/{R,N,S}/review.json` no existen y las filas anteriores no lo decían. La decisión de fondo R/N/S `NOT_REQUIRED` **no** se revierte: lo que se revierte es atribuirle un estado terminal. Volverán a `NOT_APPLICABLE` con auditoría independiente favorable de GD-12 |

Modelos configurados vigentes: explorer gpt-5.6-terra/medium; implementer
gpt-5.6-sol/medium; critic gpt-5.6-sol/high; auditor gpt-5.6-sol/xhigh;
checker gpt-5.6-luna/low. Registrar por sesión modelo y esfuerzo asignados,
efectivos cuando sean observables, sandbox nominal/efectivo y toda sustitución.
Un catálogo local no acredita carga del backend ni aislamiento.

Fuentes oficiales de formato y modelos consultadas con OpenAI Docs:
[agentes personalizados](https://developers.openai.com/fr-FR/docs/agent-configuration/subagents),
[catálogo de modelos](https://developers.openai.com/api/docs/models).
Estas fuentes establecen formato/alias, no garantizan disponibilidad por cuenta
ni imposición efectiva del sandbox en otra sesión.
## Decisión de integración — 2026-09-21

| ID | Decisión | Fundamento y alcance |
| --- | --- | --- |
| GD-24 | Integrar `origin/main` (`a65701477b19ecad172fa613aea8b8dbf94bab9c`, PR #206) en `feat/scientific-closure` mediante merge sin reescritura de historia, resolviendo cada conflicto por comparación semántica y adoptando íntegramente las correcciones auditadas del PR. **Autorización:** instrucción expresa del responsable del trabajo el 2026-09-21, que ordena resolver este merge —ya iniciado en el árbol de trabajo— y publicar el resultado **exclusivamente** en `origin/feat/scientific-closure`, y que prohíbe explícitamente `--abort`, `reset --hard`, `clean -fd`, `push --force`, rebase, modificar `main`, crear tags o releases y mergear esta rama a `main`. **Excepción acotada y declarada, no inferida:** `AGENTS.md` y `openspec/scientific-closure/README.md` excluyen merge y push «en preparación», y `plan.md` dice «sin inferir excepción ni hacer merge en esta tarea». Esta decisión **no** los enmienda ni deroga: registra que el responsable levantó esas dos exclusiones —merge de `main` hacia la rama, y push de la rama— sólo para esta operación. Toda otra exclusión (A/B/C, holdout, ledger, `main`, PR, tags, releases, UI, historia publicada) **sigue vigente sin cambios**. Si esta autorización no constara, la operación habría sido inadmisible. **Registro estructurado (hallazgo N6 de la crítica independiente):** `openspec/scientific-closure/main-integration-2026-09-21/authorizations.json` (`AUTH-MERGE-2026-09-21`), con alcance, actor, fecha, prohibiciones de la misma instrucción, base de la excepción en el criterio de aceptación de SC-GOV-003 y su limitación declarada: es la transcripción de una instrucción de sesión, no una firma verificable externamente | Las correcciones del PR #206 (contrato real de features, pisos de soporte, tratamiento monoclase, métricas indefinidas como null con razón, custodia del primer intento B, bootstrap estricto, `bootstrap_not_executed`, preflight, rutas portables del runbook) son la versión auditada de trabajo originado en esta misma rama. Tras el merge, `src/` y los tests de `controlled_daily_v4` son byte-idénticos a `origin/main`, y la rama conserva sus artefactos propios de gobernanza del cierre. **Alcance:** la decisión es de integración documental y de código. **No** altera ninguna decisión congelada anterior, **no** autoriza ejecutar A, B ni C, **no** abre el holdout 2024–2025 y **no** inicializa el ledger definitivo |
| GD-24b | Registrar que la justificación de GD-16 quedó parcialmente falsa y que su defecto pendiente sigue abierto | GD-16 se fundó en que `src/`, `docker/` y `pyproject.toml` eran byte-idénticos a `214735e…`; tras el merge eso ya no vale para `src/` (ver GD-25). La **conclusión** de GD-16 no cambia: se corrige documentación, no `src/`. El defecto que GD-16 dejó pendiente «para corregirse junto con la condición 4» **sigue sin corregir**: el docstring de `src/experiment_runner/controlled_daily_v4/__init__.py` todavía afirma que las Etapas B y C no están implementadas, lo cual es falso frente a `stage_b_runner.py`, `stage_c_runner.py` y `cli.py` (`choices=["A","B","C"]`). No se corrige en esta operación deliberadamente, para preservar la propiedad verificable de que `src/` quedó byte-idéntico a `origin/main`; corresponde a un cambio propio con su propia revisión |
| GD-25 | Redeclarar el SHA ejecutable sobre el commit resultante del merge antes de cualquier ejecución autorizada; hasta entonces, ninguna afirmación de identidad ejecutable se apoya en `214735e42ee04f018156cd630591e798aadd8bf3` | GD-04 separa SHA documental de SHA ejecutable. `214735e…` no es ancestro de `origin/main` ni del merge, y `src/` deja de ser byte-idéntico a ese commit (7 archivos de `controlled_daily_v4`). La redeclaración exige además verificar la identidad de la imagen, que sigue sin inspeccionarse en este host (EV-06). Registrado como trabajo pendiente, **no** como hecho resuelto |

## Decisiones del complemento H (HITL) — 2026-09-21

| ID | Decisión | Fundamento y alcance |
| --- | --- | --- |
| GD-26 | Escribir el archivo nuevo `src/experiment_runner/scientific_auxiliary/auxiliary_hitl_v1.py` y su prueba `tests/test_auxiliary_hitl_v1.py`, aceptando explícitamente que se debilita la propiedad «`src/` byte-idéntico a `origin/main`» que enuncian `next-session.txt` («REGLAS QUE NO CAMBIAN») y GD-25 | Son **exactamente** las dos rutas que `changes.json` declara autorizadas para `sc-08-aux-hitl` y que `proposal.md` enumera en «Archivos autorizados»: cumplir el change exige crearlas, y no hay forma de ejecutar H sin runner. La regla sobre `src/` se conserva en lo que protege de verdad y se verifica de forma más precisa: `git diff origin/main -- src/experiment_runner/controlled_daily_v4/ src/human_feedback/ pyproject.toml docker/experiment-v4/Dockerfile docker/experiment-v4/constraints.txt docker/experiment-v4/build.py` sigue **vacío**, de modo que el código que produjo A/B/C, el núcleo HU5 y la definición exacta del entorno quedan intactos. **Precisión:** `git diff origin/main -- docker/` **no** está vacío, por `docker/experiment-v4/build.ps1`, que esta rama trae desde el commit `86036d3` y que esta sesión **no** tocó; se deja constancia en lugar de enunciar una propiedad más amplia de la que se verifica. El diff completo de esta sesión contra `a742e56` son exactamente tres archivos nuevos. La única diferencia de `src/` contra `origin/main` es el directorio nuevo `scientific_auxiliary/`, que ningún artefacto de A/B/C importa. Además, la propiedad ya no protege una ejecución futura: A, B y C **se ejecutaron** el 2026-09-21 con la imagen `sha256:55bc923e…` y el commit ejecutable `214735e`, ambos congelados fuera del checkout. No se corrige de paso ningún otro defecto de `src/`: RK-19 (docstring falso de `controlled_daily_v4/__init__.py`) **sigue abierto** y sigue necesitando su propio change |
| GD-27 | Declarar para H una **identidad ejecutable propia** —rama `feat/scientific-closure-hitl-complement` e imagen `experiment-v4-hitl-complement`, construida con `docker/experiment-v4/build.py`— en lugar de atribuirle la identidad de A/B/C. El commit y el digest **exactos** que produjeron cada artefacto **no se transcriben aquí**: viven en `openspec/changes/sc-08-aux-hitl/human-package-freeze.json` y en los propios artefactos (`code_version.json`, `package-preparation-record.json`), que son la fuente autoritativa. **Corrección tras el hallazgo D-05 de la crítica independiente:** la redacción anterior fijaba aquí un commit y un digest que quedaron obsoletos al corregir los hallazgos, produciendo tres identidades distintas en tres documentos de gobernanza. Duplicar una identidad mutable en prosa la vuelve falsa en cuanto cambia | La imagen histórica `sha256:55bc923e…` **no contiene** el runner de H: se construyó el 2026-09-18, antes de que existiera. Reutilizarla es imposible y afirmar que H corrió «en la imagen de A/B/C» sería falso. La imagen de H se construye con el **mismo** `docker/experiment-v4/Dockerfile` (base `python@sha256:9534e5a8…` fijada) y las **mismas** `constraints.txt`, y se verificó que su `pip freeze --all` es **idéntico** al de la imagen histórica y que `pip check` no reporta nada: el entorno numérico es el mismo, la identidad de código no. GD-04 ya separa SHA documental de SHA ejecutable; esta decisión aplica esa separación a un experimento nuevo. **No** reconstruye ni reemplaza la imagen de A/B/C, que permanece intacta y es la única identidad de aquella campaña |
| GD-28 | Ejecutar H en **dos pistas separadas y no intercambiables** —`simulated_supervised_feedback` (el diseño congelado `auxiliary_hitl_v1`) y `controlled_human_feedback` (una intervención humana controlada del responsable)— en lugar de sustituir una por otra | El criterio de aceptación congelado de SC-GOV-022 exige «Tres brazos y maduración separados, 20 eventos/seed conforme diseño H; **simulación identificada**», y CL-06 limita la afirmación a «correcciones supervisadas **simuladas**; nunca beneficio de una persona real». Sustituir la simulación por la intervención humana **incumpliría** esa condición congelada, y omitir la intervención humana incumpliría la instrucción vigente del responsable. Las dos pistas resuelven ambas cosas sin relajar ninguna: la pista SIM satisface SC-GOV-022 y CL-06 tal como están escritas, y la pista HUMANA existe porque el propio diseño congelado la contempla al decir «**Feedback real futuro necesita protocolo de revisión y procedencia propia**» — el contrato `contract-H-frozen.json` ES ese protocolo propio. La pista humana **no** sostiene ninguna afirmación cuantitativa y **no** se etiqueta nunca como `expert_agronomic_feedback` |
| GD-29 | Registrar la ausencia de intervención agronómica como **limitación y trabajo futuro**, no como evidencia faltante que bloquee H | Ningún documento vigente exige la participación de un agrónomo para cerrar HU5, el complemento H ni el gate GF. Al contrario: `openspec/project.md` la excluye del alcance («No incluye … validación agronómica longitudinal multi-campaña»); ADR-0011 declara que Pergamino es reanálisis y «nunca validación agronómica in situ»; `claims.md` prohíbe afirmar «eficacia agronómica»; y el propio diseño congelado de H ordena «No afirmar feedback humano genuino ni eficacia agronómica». La única casilla pendiente del repositorio que nombra a un agrónomo (`openspec/changes/simplify-producer-ui/tasks.md`) es de **comprensión de la interfaz**, y su propio `proposal.md` declara que no altera el alcance científico. La limitación se declara de forma explícita y permanente en la evidencia de H y no se compensa por inferencia. **Corrección tras el hallazgo AUD-H-08 de la auditoría final:** la redacción anterior citaba también un `README.md` que excluiría la validación agronómica de los requisitos de la campaña. El auditor verificó que ese texto **no existe** ni en `README.md` ni en `openspec/scientific-closure/README.md`, y la cita se retira. Las otras cuatro citas sí fueron corroboradas una por una y sostienen la conclusión de fondo. El propio operador declaró además, con sus palabras y sin que se le preguntara por ello, que la validación de campo con una persona que observe si las predicciones se corresponden con la realidad es trabajo futuro |

## Decisiones del cierre documental de la evidencia — 2026-09-22

| ID | Decisión | Fundamento y alcance |
| --- | --- | --- |
| GD-30 | Cerrar `GD-12` declarándola criticada y auditada de forma independiente, sin modificar su contenido de fondo: R `NOT_REQUIRED`, N `NOT_REQUIRED`, S `NOT_REQUIRED`, H `REQUIRED`. En consecuencia, `SC-GOV-021`, `SC-GOV-023` y `SC-GOV-024` vuelven de `BLOCKED` a `NOT_APPLICABLE` **como requisitos** | `GD-23` degradó esas tres filas por una causa exacta y nombrada: la decisión de suficiencia «sigue sin crítica ni auditoría independiente» y los artefactos `auxiliary/{R,N,S}/review.json` no existían. Ambas condiciones dejaron de valer: los tres artefactos existen en `evidence-finalization-2026-09-22/auxiliary/`, y la revisión independiente final de esta sesión los critica y los audita, y su informe se preserva verbatim en `evidence-finalization-2026-09-22/reviews/review-audit-final.md`. **Precisión tras el hallazgo `F-02` de esa misma revisión:** la primera redacción de `GD-30` daba por hecha esa crítica y esa auditoría mientras los cuatro artefactos declaraban `independent_review: PENDING`. La decisión sólo rige desde que el informe existe y su veredicto quedó registrado en cada artefacto, no antes. `GD-23` anticipó exactamente este desenlace al escribir que volverían a `NOT_APPLICABLE` «con auditoría independiente favorable de GD-12». **Lo que no cambia:** la anterioridad de GD-12 a la ejecución, verificable en git desde el 2026-09-19, no depende de los artefactos escritos hoy; y `NOT_REQUIRED` sigue limitando la afirmación en lugar de eliminar el requisito de tesis. **Limitación declarada:** por instrucción del responsable esta sesión solicita **una sola** revisión independiente, de modo que crítica y auditoría de GD-12 las ejecuta el mismo lector en una sola pasada. No fueron dos lectores distintos y no se presenta como si lo fueran |
| GD-31 | **No** modificar `scripts/check_scientific_closure.py` para admitir la transición `BLOCKED → NOT_APPLICABLE`, y dejar los changes `sc-07-aux-regression`, `sc-09-aux-anomalies` y `sc-10-aux-robustness` en `BLOCKED` aunque sus requisitos pasen a `NOT_APPLICABLE` | La máquina de estados del checker sólo admite `PLANNED → NOT_APPLICABLE`, de modo que no existe camino desde `BLOCKED`. Habría bastado añadir una arista. **No se hizo, deliberadamente:** relajar la herramienta de verificación para que acepte el estado que se desea registrar es la forma exacta de fallo que este sistema existe para impedir, y el hallazgo `AUD-F-02` ya condenó su análogo documental. La asimetría además **no es un artificio**: el estado del *requisito* describe si la obligación condicional aplica al alcance aprobado, y el del *change* describe si se implementó y ejecutó. El registro de estado de los tres changes ya decía, desde el 2026-09-19, que «su decisión NOT_REQUIRED auditada no equivale a ejecución ni cierre individual del change». La asimetría queda declarada en la matriz final en lugar de disimularse |
| GD-32 | Emitir la síntesis científica de los resultados efectivamente obtenidos como documento **nuevo**, `docs/research/scientific-closure-synthesis-2026-09-22.md`, en lugar de reescribir el del 2026-09-20 | El documento del 2026-09-20 declara de sí mismo que «no contiene ningún resultado científico, porque ninguna campaña fue ejecutada». Es un registro fechado y correcto de su momento, y la regla de no reescribir documentos históricos lo protege. El nuevo documento supersede al anterior **sólo** en lo relativo a resultados; las secciones de procedencia, licencias y diseño de aquél siguen vigentes y no se duplican |
| GD-33 | Declarar el gate `GF` en `PASS_WITH_LIMITATIONS` y `SC-GOV-025` en `PASS_WITH_LIMITATIONS`, no en `PASS` pleno ni en `BLOCKED` | Los cinco obstáculos que enumeró el auditor final del complemento H —`RB-02` a `RB-06`— eran documentales y de revisión, y él mismo verificó que **ninguno exigía reabrir el holdout ni reejecutar A, B o C**. Los cinco están resueltos en esta sesión. Mantener `BLOCKED` sería sostener un bloqueo cuya causa se resolvió. **Y tampoco es `PASS` pleno:** diez requisitos quedan en `PASS_WITH_LIMITATIONS`, nueve de ellos distintos de `SC-GOV-025` mismo, y conservan limitaciones materiales no subsanadas —sustitución de perfiles de rol (RK-10), desviación de push RK-14 no curada, condición 2 de ADR-0011 `PENDING_CONFIRMATION`, resultados sin ancla en git, multiplicidad sin corregir, y las seis limitaciones vivas del complemento H—. Ninguna de ellas requiere evidencia científica nueva; todas quedan declaradas y ninguna se atenúa. El cierre alcanza al alcance aprobado de HU7/HU8 y **no** certifica HU1 ni la tesis completa |
| GD-34 | Bajar `SC-GOV-003`, `SC-GOV-008` y `SC-GOV-018` de `PASS` a `PASS_WITH_LIMITATIONS` en la matriz final. **Corrección tras el hallazgo `F-04`:** la redacción original incluía también `SC-GOV-016`, y era falsa: ese requisito **ya figuraba** `PASS_WITH_LIMITATIONS` desde el 2026-09-21, de modo que cambia de base y no de estado. Son tres descensos, no cuatro | Sus limitaciones existían y estaban registradas —en `decisions.md`, en `risks.md` o en la auditoría de H— pero **no en su propia fila de la matriz**, de modo que el recuento del 2026-09-21 (14 PASS) leía mejor de lo que la evidencia sostiene. Degradar tres filas en la misma sesión en que se cierran cinco bloqueos es deliberado: impide que el cierre parezca una mejora uniforme. Ningún hecho nuevo lo motiva; motiva la ubicación correcta de hechos ya conocidos |
| GD-35 | Registrar como limitación de procedimiento, no subsanada, que esta sesión ejecutó **una sola** revisión independiente en lugar de la secuencia checker → crítico → auditor que fija `operations.md` | Es instrucción expresa del responsable en el encargo vigente («Solicitá una sola revisión independiente final»), fundada en que esta fase no incorpora evidencia experimental nueva y en que las verificaciones, auditorías, hashes y respaldos previos se toman como válidos salvo contradicción concreta. La excepción es **acotada y declarada, no inferida**, y no enmienda `operations.md`: registra que el responsable la levantó para esta operación. Toda otra exclusión sigue vigente sin cambios |

## Decisiones posteriores a la revisión independiente — 2026-09-22

| ID | Decisión | Fundamento y alcance |
| --- | --- | --- |
| GD-36 | Aceptar el `FAIL` documental del auditor independiente y corregir los tres hallazgos materiales `F-01`, `F-02` y `F-03` antes de registrar `GF` y `SC-GOV-025`, en lugar de discutirlos | Los tres son sobreafirmaciones en el registro, no defectos de la ciencia, y el auditor lo dice con todas las letras: recomputó cerca de sesenta cifras desde la evidencia con cero discrepancias. `F-01` es la reincidencia literal de `AUD-F-01` —acreditar un artefacto inexistente y prejuzgar al auditor— en la misma fila de requisito y dos días después. `F-03` es la reincidencia de `EV-02` —un resumen que su propia tabla desmiente—. Discutirlos habría sido exactamente el modo de fallo que este proyecto tiene documentado. **El informe se preserva verbatim con su veredicto `FAIL` intacto**, junto al informe de la revisión de la corrección; no se reescribe ni se sustituye por el resultado favorable posterior |
| GD-37 | Registrar que el propio orquestador había detectado `F-03`, `F-04` y `F-05` **antes** de recibir el informe, y que aun así los dejó en el snapshot auditado | Corregirlos sobre la marcha habría alterado el árbol mientras el lector independiente trabajaba sobre el commit `7e63d1c`, produciendo una discrepancia entre lo que `git show` mostraba y lo que el disco contenía. Se optó por congelar el snapshot y corregir en la ronda de corrección, que es el procedimiento que el encargo fija. **Consecuencia que se declara en lugar de omitirse:** tres de los nueve hallazgos del informe eran conocidos por el implementador, de modo que la revisión independiente **no** es la única fuente que los detectó, y no debe acreditársele a ella sola el mérito de encontrarlos. Los otros seis —incluidos los dos materiales `F-01` y `F-02`— sí los encontró el auditor y sólo él |
| GD-38 | Declarar `AF-05` y `AF-06` como puntos `BLOCKED` por punto, no como `PASS` por confianza | `INV-10` (las 74 pruebas focalizadas de H) y la reproducción estricta 18/18 siguen sin verificación independiente: ni la auditoría de H (`AUD-H-09`) ni ésta pudieron ejecutarlas. Y el auditor informa que de los 91 archivos que cubre el manifiesto sólo pudo leer **14** en su ubicación original y que **75** le resultaron ilegibles por ser `root:600`, de modo que su verificación de esos 75 descansa en el respaldo, que sí verificó entero (91/91 OK). Los 14 legibles coincidieron exactamente. Se transcriben sus dos cifras tal como las dio, sin reconciliarlas con el total; la imagen `sha256:55bc923e…` tampoco fue inspeccionada. Ninguno de estos puntos bloquea `GF`, y ninguno debe presentarse como auditado |
