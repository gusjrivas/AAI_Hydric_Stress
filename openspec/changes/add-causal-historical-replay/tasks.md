# Tareas — add-causal-historical-replay

El candidato `base-seed4` quedó habilitado para implementación tras la
revisión dirigida (`paso1-revision-dirigida.md`); las secciones 1 y 2
(paquete, lector, validaciones) se completaron en el Paso 2, en dos pasadas
(`paso2-paquete-lector.md`, `paso2-correccion-validaciones.md`). El Paso 3
(`paso3-api-lectura.md`) corrigió un defecto real de admisión y agregó la
API mínima de solo lectura. El Paso 4 (`paso4-interfaz-feedback.md`) cerró
la incertidumbre de regresión heredada del Paso 2 (ambos fallos eran
ambientales, no del código de este *change*), agregó `GET /replay/predictions`
(orígenes disponibles), el feedback de demostración completo (RH-07,
backend + frontend), y la pantalla "Reproducción histórica" — verificada
en navegador real, no solo por tests. El Paso 4.1 (`paso4-1-cierre-demo.md`)
corrigió defectos concretos de aislamiento de estado, errores explícitos,
lenguaje/presentación y arranque reproducible, detallados en la sección
correspondiente más abajo. Orden: paquete y lector → validaciones → API →
interfaz → feedback → verificación del recorrido y capturas → cierre
funcional (Paso 4.1).

**Paquete vigente: `replay_packages/base-seed4-1157696b7b-v2/`**
(`schema_version=historical_replay_package_v2`). El paquete anterior
(`replay_packages/base-seed4-1157696b7b/`, `_v1`) se conserva intacto, sin
sobrescribir ni borrar, como registro histórico de la primera versión — ya
no es el que debe usarse (no tiene `run_metadata.json` ni
`admission_contract`, exigidos por el lector corregido).

## 1. Paquete y lector

- [x] Construir el manifiesto de hashes del paquete de lectura reproducible (`design.md` §7), con metadatos mínimos verificables de padre e hijo (`run_metadata.json`: IDs, experimentos, estado, `mlflow.parentRunId`, parámetros pertinentes, commit) y un contrato de admisión explícito (`admission_contract.verified_candidates`). Implementado en `scripts/build_replay_package.py`; paquete real generado en `replay_packages/base-seed4-1157696b7b-v2/manifest.json`; construido dentro de un directorio temporal propio y publicado solo tras pasar `load_package` (validar-antes-de-publicar); verificado con `tests/test_historical_replay_package_loader.py`.
- [x] Rechazar la construcción si el run padre no declara `dataset_sha256` — ya no se sustituye por un hash calculado hoy (`scripts/build_replay_package.py::ProvenanceMissingError`). Probado indirectamente por el contrato del lector (`test_missing_historical_hash_is_rejected_never_substituted`).
- [x] Implementar el módulo de lectura de runs v3 (`experiment_id`, `run_id` → `HistoricalPredictionRecord` tipado), resolviendo la procedencia siguiendo `mlflow.parentRunId`, sin deserializar el modelo, y contrastando esos metadatos contra el manifiesto y `effective_configuration.json` (no solo confiando en el manifiesto). Implementado en `src/historical_replay/package_loader.py` + `src/historical_replay/records.py`; probado en `tests/test_historical_replay_records.py`, `tests/test_historical_replay_package_loader.py`.
- [x] Recomputar `interpolate_missing_causal`/`validate_daily_series` sobre el dataset ya verificado por hash para derivar el marcador `<columna>_imputado` del historial pre-corte, con verificación explícita de equivalencia de fuente contra el commit de ejecución (no solo importar la versión actual sin comprobarla). Implementado en `src/historical_replay/imputation_markers.py` (`verify_imputation_source_matches_verified_commit`), probado en `tests/test_historical_replay_imputation_markers.py`; incorporado al paquete real.

## 2. Validaciones

- [x] Rechazo de identidad de predicción duplicada (RH-01), incluida la duplicación por representaciones equivalentes de una misma fecha (`"2024-10-19"` vs. `"2024-10-19T00:00:00.000"`), normalizadas antes de comparar. Implementado en `src/historical_replay/records.py::build_records`; probado en `tests/test_historical_replay_records.py`.
- [x] Elegibilidad temporal completa de RH-11: `training_dates` ausente/vacío/mal formado se rechaza explícitamente (`TrainingDatesError`); madurez de cada etiqueta de entrenamiento respecto del corte (`TrainingMaturityError`); origen de cada predicción igual o posterior al corte autorizado (`PredictionBeforeCutoffError`); concordancia de horizonte y corte entre manifiesto/`effective_configuration.json`/`run_metadata.json` (`TemporalConcordanceError`). Implementado en `src/historical_replay/package_loader.py`; probado en `tests/test_historical_replay_package_loader.py`.
- [x] Identidad de modelo, **corregida en el Paso 3**: la primera versión (`_verify_admission_contract`) leía `verified_candidates` del mismo manifiesto que estaba validando — un candidato autodeclarado podía admitirse a sí mismo con solo incluirse en su propia lista, y `build_replay_package.py` generaba esa autodeclaración para cualquier `run_id_child` recibido como argumento, sin restringirla. Corregido con una política mantenida fuera del paquete, en código versionado (`src/historical_replay/admission_policy.py::ADMITTED_CANDIDATES`, un único candidato hardcodeado, no un registro extensible), aplicada tanto en el lector (contra `run_metadata.json`, no contra el manifiesto) como en el constructor (antes de descargar ningún artefacto). Los valores del candidato admitido (experimento, run hijo/padre, config, semilla, commit, `dataset_sha256`) se corroboraron contra `paso1-revision-dirigida.md` antes de incorporarlos: sin discrepancias. Probado en `tests/test_historical_replay_admission_policy.py`, `tests/test_historical_replay_package_loader.py::test_unknown_candidate_is_rejected_even_if_everything_agrees_internally` (rechaza un candidato cuyo manifiesto, `verified_candidates` propio y `run_metadata.json` concuerdan perfectamente entre sí pero no son el admitido), `tests/test_build_replay_package.py` (con un cliente MLflow simulado, sin red).
- [x] Rechazo de vinculación cruzada de observación (RH-12): nueva entidad `LinkedObservation` (`src/historical_replay/observations.py::link_observation`), que rechaza series/dataset que no coincidan con el declarado, fechas objetivo ausentes de la serie, y ambigüedad por fechas duplicadas; distingue `medida`/`imputada`/`no_determinado`/`sin_dato_en_fuente` sin fusionarlos, y detecta la inconsistencia `target_observed=true` sin valor crudo disponible (`ObservationConsistencyError`). Probado en `tests/test_historical_replay_observations.py`.
- [x] Historial filtrado por reloj: `src/historical_replay/history_view.py::filtered_history` — nunca expone el DataFrame completo, solo filas hasta la fecha simulada. Probado en `tests/test_historical_replay_history_view.py`.
- [x] Separación de los dos umbrales (RH-13), con test dirigido propio y aislado (antes solo verificado por inspección del manifiesto). El manifiesto real declara `label_rule.frozen_threshold=0.31678178906440735` m3/m3 vs. `decision_rule.alert_threshold=0.5`, con procedencia y unidad distintas.
- [x] Validación estricta de tipos y coherencia por fila (`records.py::build_records`): `target_observed` debe ser `bool` real (rechaza `"true"`, `1`); `y_proba` numérico finito en `[0, 1]`; `y_pred`/`y_true` en clases admitidas `{0, 1}`; coherencia `target_observed` ↔ presencia de `y_true`; horizonte positivo; identidad por fila que no puede sustituir silenciosamente la identidad autorizada (`ForeignIdentityError`) ni faltar sin más (`MissingIdentityError`, nunca convertida en el string `"None"`). `baselines` es un `MappingProxyType` inmutable (un dataclass `frozen=True` no protege un dict anidado) y la proyección pública sigue devolviendo una copia independiente. Probado extensamente en `tests/test_historical_replay_records.py`, `tests/test_historical_replay_projection.py`.

## 3. API (backend)

- [x] Implementar `ReplayPredictionView` como proyección pura de `(HistoricalPredictionRecord, fecha_simulada)` (RH-02–RH-04). Implementado en `src/historical_replay/projection.py::project`; probado en `tests/test_historical_replay_projection.py` (9 escenarios, incluyendo salto sobre la fecha objetivo y retroceso/reavance).
- [x] Implementar el endpoint de solo lectura del candidato (`GET /replay/candidate`): experimento/run/config/semilla, horizonte +3 días, rango autorizado del recorrido (`periodo_inicio`/`periodo_fin`), limitaciones y disclaimers (reproducción retrospectiva, proxy estadístico relativo, utilidad agronómica no demostrada). Implementado en `backend/app/routers/replay.py::get_candidate`; probado en `backend/tests/test_replay.py`.
- [x] Implementar el endpoint de solo lectura de una predicción (`GET /replay/predictions/{timestamp_origen}?simulated_date=...`), con el contrato temporal corregido (predicción visible desde el origen; objetivo/`y_true`/`target_observed`/coincidencia/medición original solo desde la fecha objetivo, nunca antes, ni como campos `null`); vincula la observación con `link_observation` usando el dataset del propio paquete cargado (nunca dos nombres provistos por el cliente). `y_proba` se omite deliberadamente de la respuesta. Implementado en `backend/app/routers/replay.py::get_prediction` + `backend/app/schemas_replay.py`; probado en `backend/tests/test_replay.py` (predicción oculta antes del origen, visible sin campos futuros, revelación exacta en el objetivo, retroceso vuelve a ocultar, identidad desconocida rechazada).
- [x] Implementar el endpoint de historial filtrado por reloj (`GET /replay/history?simulated_date=...`), reutilizando `historical_replay.history_view.filtered_history` — nunca expone el dataset completo. Implementado en `backend/app/routers/replay.py::get_history`; probado en `backend/tests/test_replay.py::test_history_is_filtered_by_the_simulated_clock`.
- [x] El backend carga exclusivamente el paquete autorizado desde una ubicación configurada por el servidor (`backend/app/config.py::get_historical_replay_package_dir`, `HISTORICAL_REPLAY_PACKAGE_DIR`), nunca desde una ruta indicada por el cliente — mismo patrón que `get_producer_bundle_root`. Feature flag apagado por defecto (`HISTORICAL_REPLAY_ENABLED`, `backend/app/dependencies.py::require_historical_replay_enabled`), igual que `PRODUCER_V2_ENABLED`. Probado (`test_disabled_by_default_returns_not_found`, `test_self_declared_unauthorized_candidate_is_rejected`).
- [x] La API funciona sin MLflow ni red durante la reproducción (probado bloqueando `socket.create_connection`) y no modifica los artefactos originales del paquete (probado comparando hashes de los archivos antes/después de varias solicitudes).
- [x] Endpoint de metadatos mínimos de orígenes disponibles (`GET /replay/predictions`), sin observaciones ni resultados futuros — permite a la interfaz ofrecer otros orígenes reales sin inventar fechas. Implementado en `backend/app/routers/replay.py::list_origins`; probado en `backend/tests/test_replay.py::test_list_origins_returns_only_minimal_metadata`.
- [x] Ficha de trazabilidad expandible servida por la API (`ReplayEvidenceCard` en `/replay/candidate`): `package_id`, nombre del dataset, commit, corte de entrenamiento, convención de fecha, supuesto de disponibilidad diaria — campos explícitos, nunca el manifiesto completo. Probado en `test_candidate_includes_expandable_evidence_card`.
- [ ] Regla determinística de selección de la fecha *inicial* del recorrido y `prediccion_seleccionada_id` como concepto de API (RH-10): **resuelto en la interfaz, no en el backend** (ver sección 4) — la API sigue requiriendo `timestamp_origen` explícito; la interfaz calcula la selección inicial (primer origen disponible) y la mantiene fija en su propio estado.
- [x] Manejo explícito de `target_observed=false` (RH-03/RH-05): estado sin comparación, nunca fabricada — implementado tanto en `projection.py::project` como en el endpoint de predicción. Sigue sin ejercitarse con datos reales de `base-seed4` (0/67 filas), solo con casos construidos en los tests.
- [x] **RH-07, backend:** `src/historical_replay/feedback.py` (`ReplayFeedbackStore`, `register_feedback`, `visible_feedback_for`) — almacenamiento exclusivo (JSON-lines, un archivo por `package_id`, en `replay_feedback/`, nunca `data/feedback__<sensor_id>.parquet` ni dentro de `replay_packages/`); valida identidad de la predicción referenciada (`UnknownPredictionError`), contenido (`InvalidFeedbackContentError`), y exige que la observación ya haya sido revelada (`FeedbackNotYetRevealedError`) — el servidor no confía en que el cliente haya esperado. `registered_at` (real) y `simulated_at` (simulada) siempre separados; nunca atribuido a un experto ni a una fecha histórica. Endpoints `POST`/`GET /replay/predictions/{origen}/feedback`. Probado en `tests/test_historical_replay_feedback.py` (7 tests) y `backend/tests/test_replay.py` (4 tests: rechazo antes de revelar, registro visible y aislado en su propio archivo, oculto tras retroceder sin borrarse, identidad desconocida rechazada).

## 4. Interfaz (frontend)

**Implementada** (`frontend/src/features/historical-replay/`), ruta
`#reproduccion-historica` (enlace visible junto al de "Demostración"),
consumiendo exclusivamente las respuestas ya filtradas de la API — nunca
descarga el paquete completo ni el dataset entero.

- [x] Vista principal del recorrido: selección determinística inicial (primer origen de `GET /replay/predictions`), predicción visible desde su origen, clase predicha con su significado de "proxy estadístico relativo", sin mostrar `y_true`/observación antes de la revelación. `HistoricalReplayPage.tsx`.
- [x] Controles de avance/retroceso/reinicio del reloj — un `requestKey` por `(origen, fecha simulada)` descarta respuestas HTTP tardías de un corte anterior (probado con una promesa controlada manualmente: `HistoricalReplayPage.test.tsx::ignores a late response...`). Retroceder oculta observación y feedback ya revelados sin alterar la predicción archivada (verificado también en navegador real).
- [x] Estados de carga, error, "no disponible" (feature apagada en el backend), predicción no encontrada y observación no disponible — nunca completados por estimación.
- [x] Selector de otros orígenes realmente disponibles (`<select>` poblado por `GET /replay/predictions`, solo fechas, sin observaciones).
- [x] Historial de humedad limitado al reloj simulado, con huecos de datos preservados como cortes reales del trazo (`MoistureHistoryChart.tsx`) — no unidos ni presentados como medición.
- [x] Lenguaje y trazabilidad (Paso 4 §4): reproducción retrospectiva, proxy estadístico, utilidad agronómica no demostrada — mensaje fijo visible; ficha expandible (`ReplayEvidenceCard.tsx`) con origen de datos, identidad de modelo/run, corte de entrenamiento, horizonte, identificación del paquete, supuesto de disponibilidad, limitaciones.
- [x] `y_proba` **nunca se muestra** — decisión explícita del Paso 3 mantenida aquí (la API ya no la expone en absoluto); no hay "detalle técnico" con `y_proba`, por lo tanto ese ítem del Paso 4 §4 se resolvió por omisión total, no por advertencia acompañante.
- [ ] Distinguir visualmente `medida`/`imputada`/`no_determinado`/`sin_dato_en_fuente` en el **historial pre-corte** (RH-05/RH-08): la API de historial (`GET /replay/history`) todavía no expone ese estado por fila (solo `soil_moisture: number | null`); la interfaz sí distingue estos estados para la **medición del objetivo ya revelado** (`medicion_original.estado`), pero no para cada punto del historial. Pendiente de una extensión de `ReplayHistoryRow` en la API (fuera de alcance de este paso, no bloqueante).

## 5. Feedback

**Implementado** (backend en la sección 3; interfaz aquí).

- [x] Formulario de feedback de demostración (`ReplayFeedbackForm.tsx`), habilitado únicamente cuando la predicción revela observación (`target_observed !== undefined`); estados "coincide"/"no coincide", etiqueta corregida opcional, observación opcional; muestra `registered_at`/`simulated_at` por separado si ya existe un registro. Verificado con pruebas y **en navegador real** (ver `paso4-interfaz-feedback.md`).
- [x] Aislamiento arquitectónico: `ReplayFeedbackStore` nunca es importado ni referenciado por `human_feedback/*` — probado indirectamente por construcción (módulos sin dependencia mutua) y por la ubicación de almacenamiento separada (`replay_feedback/`, no `data/`).

## 6. Verificación del recorrido y capturas

- [x] Recorrido completo verificado **en navegador real** (Chrome, vía una instancia Docker aislada y descartable de la misma imagen, con este worktree montado — nunca el contenedor compartido `aai-hydric-stress-backend-1`): antes de revelar, revelado con discrepancia real (`y_pred=0` vs. `y_true=1`), feedback registrado, y retroceso ocultando de nuevo observación y feedback. Capturas reales guardadas (ver `paso4-interfaz-feedback.md`).
- [ ] Redactar en la memoria (capítulo 3): pendiente, no se tocó ningún capítulo en este paso (instrucción explícita del Paso 4 §7: "no proponer modificar el capítulo 2 ya corregido").
- [ ] Exportar el paquete de lectura para uso en la defensa (requiere autorización explícita separada de este *change*).
- [ ] Extender `GET /replay/history` con el estado de medición por fila (nota de la sección 4) — pendiente, no bloqueante.

## 7. Cierre funcional de la demo (Paso 4.1)

Detalle completo en `paso4-1-cierre-demo.md`.

- [x] Aislamiento de estado por generación (no por clave `origen|fecha`, que podía repetirse en A→B→A): `HistoricalReplayPage.tsx` usa un contador `generationRef` que se incrementa en cada cambio de `(origen, fecha simulada)`, limpia predicción/historial/feedback/errores en el mismo efecto que dispara las nuevas consultas, y descarta cualquier respuesta (incluida la de `createFeedback`) cuya generación ya no sea la vigente. El formulario se remonta (`key={origen|fecha}`) al cambiar de predicción. El formulario se habilita exclusivamente cuando `target_observed === true` (antes se habilitaba también con `target_observed === false`, un defecto real corregido aquí). Probado con 8 pruebas dirigidas nuevas de promesas controladas en `HistoricalReplayPage.test.tsx`. **Corregido en el Paso 4.1.1** (ver sección 8 abajo): esta limpieza vivía en un `useEffect`, que corre después del render — no era la garantía síncrona previa al render que el informe original describía.
- [x] Errores explícitos: historial y feedback distinguen ahora `loading`/`error`/`ready` (antes un fallo HTTP se convertía silenciosamente en "sin datos"); un error al consultar el feedback existente ya no habilita silenciosamente un nuevo registro. Probado en `HistoricalReplayPage.test.tsx`.
- [x] Semántica y presentación: reemplazadas las etiquetas "Estrés/Sin estrés" por lenguaje derivado de la regla verificada del proxy ("por debajo del umbral de humedad" / "no inferior al umbral de humedad"), expuesta ahora por la API (`ReplayCandidateInfo.regla_etiqueta`, `backend/app/routers/replay.py::_label_rule`) y verificada contra `manifest.label_rule` antes de traducirla (nunca inventada ni invertida). Selector operativo "sensor-a" y controles operativos ocultos en la ruta de reproducción histórica (`App.tsx`). Ejes legibles (fechas y m³/m³) agregados al gráfico (`MoistureHistoryChart.tsx`). Ficha de trazabilidad distingue ahora `split_date` (corte de partición) de `training_max_date` (última fecha realmente usada para entrenar, `2024-10-15` — anterior al corte, `2024-10-19`, en este candidato): expuesto por la API (`ReplayEvidenceCard.training_max_date`, derivado de `effective_configuration.json::training_dates`).
- [x] Arranque reproducible: `scripts/demo_replay_up.sh`/`demo_replay_down.sh`. **Reescritos en el Paso 4.1.1** (ver sección 8): la versión de este paso nunca se había ejecutado de verdad (asumía Python/Node locales, no disponibles en este host); ahora orquestan los dos contenedores Docker desechables ya verificados en el Paso 4, detienen por nombre de contenedor (no por PID) y comprueban disponibilidad real antes de anunciar éxito.
- [x] Verificación acotada: pruebas dirigidas con promesas controladas (ver arriba) + recorrido real en navegador (instancia Docker descartable, nunca el contenedor compartido): antes de revelar → revelado con discrepancia real → feedback registrado → retroceso ocultando observación y feedback sin borrar el registro persistido. Capturas nuevas en `paso4-1-capturas/`.

## 8. Cierre de dos pendientes concretos (Paso 4.1.1)

Detalle completo en `paso4-1-1-cierre.md`.

- [x] Garantía de render corregida: `selectionScope.ts` (`taggedFor`/`visibleFor`, puro, 5 pruebas unitarias directas) reemplaza la limpieza "síncrona" inexistente del Paso 4.1 — cada resultado se etiqueta con `(origen, fecha)` y el render descarta lo que no corresponda a la selección vigente, sin depender de que el `useEffect` haya corrido. Regresión de `mountedRef` bajo `StrictMode` encontrada verificando en navegador real (no por ningún test previo) y corregida, con test dirigido que reproduce el defecto en RED antes de corregirlo.
- [x] Arranque único realmente probado: scripts reescritos para Docker (ver arriba), ejecutados tal cual (sin comandos alternativos) con dos sesiones reales (`sesion-a`, `sesion-b`): a) cambio entre dos orígenes reales, b) feedback registrado en `sesion-a`, c) sesión detenida y `sesion-b` arrancada vacía, d) feedback de `sesion-a` preservado en disco y ausente en `sesion-b`. Defecto real encontrado y corregido en el propio chequeo de disponibilidad (`curl -f` reportando fallo pese a un 200 real).
- [x] Corregida una afirmación no respaldada del Paso 4.1 sobre concurrencia del almacenamiento de feedback (no hay ningún test de escritura concurrente) — ver `paso4-1-1-cierre.md` §3.
