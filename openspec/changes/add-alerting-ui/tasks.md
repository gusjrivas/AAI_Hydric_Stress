# Tareas — add-alerting-ui

Primer scaffolding de `backend/` y `frontend/` (ADR-0003), exponiendo HU5 (retroalimentación) y HU6 (pipeline completo) a través de una interfaz de usuario.

- [x] Scaffolding de `backend/` (FastAPI) con configuración de dataset por variable de entorno. Evidencia: `backend/app/config.py` (`ALERTING_UI_DATASET`, `ALERTING_UI_FEEDBACK_LOG`), `backend/app/main.py`, `backend/app/schemas.py`.
- [x] Endpoint `POST /forecast/run`. Evidencia: `backend/app/routers/forecast.py`; testeado en `backend/tests/test_forecast.py`.
- [x] Endpoints `GET /feedback`, `POST /feedback/{fecha}/confirm`, `POST /feedback/{fecha}/reject`. Evidencia: `backend/app/routers/feedback.py`; testeados en `backend/tests/test_feedback.py`.
- [x] Tests de backend con `TestClient` contra la app real, `data_dir` temporal inyectado. Evidencia: `backend/tests/test_forecast.py` y `backend/tests/test_feedback.py`, ambos usan `app.dependency_overrides[get_feedback_data_dir] = lambda: tmp_path`. 7 tests de backend pasando.
- [x] Scaffolding de `frontend/` (Vite + React + TypeScript). Evidencia: `frontend/` (Vite React-TS template + `vitest.config.ts`, `src/setupTests.ts`).
- [x] Página de pronóstico y alertas (botón de correr pronóstico, tabla, acciones confirmar/rechazar). Evidencia: `frontend/src/features/forecast/ForecastPage.tsx`, `frontend/src/features/forecast/api.ts`.
- [x] Tests de frontend con React Testing Library + Vitest. Evidencia: `frontend/src/features/forecast/ForecastPage.test.tsx`, 2 tests pasando.
- [x] Verificación manual end-to-end (backend + frontend corriendo, navegador) con el dataset real. Evidencia: backend (`uvicorn app.main:app --port 8000`) y frontend (`npm run dev`, `http://localhost:5173`) corridos juntos contra `data/melchor_romero_2024_consolidado.parquet`: tabla con 71 fechas reales (2024-10-19 a 2024-12-28), `train_rows=286`, `test_rows=71`, 22 de 71 alertas (`alerta: true`). Confirmar/rechazar en 2024-10-19/2024-10-20 actualizó su "Estado" de inmediato; al re-correr el pronóstico ambas fechas conservaron su estado validado y las 69 restantes volvieron a `pendiente`, confirmando `upsert_feedback_log` de punta a punta. Ver `openspec/specs/alerting-ui/spec.md` para el detalle completo.
- [x] Actualizar CI (`.github/workflows/ci.yml`) con jobs acotados a `backend/`/`frontend/`, sin modificar el job de Python existente (ADR-0003). Evidencia: jobs `backend-quality` y `frontend-quality` agregados en `.github/workflows/ci.yml`, después de `python-quality` (sin tocarlo); YAML validado con `python -c "import yaml; yaml.safe_load(open('.github/workflows/ci.yml'))"`.
- [x] Actualizar `docs/seguimiento-tareas.md` y las specs vigentes (`architecture-integration`, `human-feedback`, nueva `alerting-ui`). Evidencia: nueva sección "Interfaz de usuario (alerting-ui, HU5+HU6)" en `docs/seguimiento-tareas.md`; nueva `openspec/specs/alerting-ui/spec.md`; notas de puntero agregadas en `openspec/specs/architecture-integration/spec.md` y `openspec/specs/human-feedback/spec.md`.
