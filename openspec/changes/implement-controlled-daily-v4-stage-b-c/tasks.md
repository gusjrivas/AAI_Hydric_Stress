# Tareas — implement-controlled-daily-v4-stage-b-c

**Actualización (2026-09-14, corrección de revisión dirigida sobre el PR #193,
commit auditado `d05f80e6`):** cuatro hallazgos concretos sobre el runner/CLI/
escritor de la Etapa B (protección de los artefactos de A ante `--output-dir`
efectivamente coincidente con `--producer-dir`; persistencia del veredicto
`CANDIDATE_NOT_VALIDATED` con datos genuinamente monoclase, antes perdida por
un `ValueError` en el escritor; cobertura/calendario completos del período
evaluable verificados ANTES de tocar el estimador, antes solo parcialmente
verificados; y evidencia de reproducibilidad de B -- `constraints_identity`,
advertencias de ajuste, diagnósticos de bootstrap con cero réplicas válidas)
fueron corregidos en la misma rama/PR, sin reabrir ninguna decisión bloqueada
ni cambiar familia/hiperparámetros/bootstrap normativo/fronteras del
protocolo. Ver `docs/seguimiento-tareas.md`, entrada "Corrección de cuatro
hallazgos de revisión dirigida sobre la Etapa B de controlled_daily_v4
(2026-09-14)", para el detalle completo y los conteos de pruebas. Esta
corrección es documental/de manejo de casos límite sobre el código YA
implementado descrito debajo -- no reemplaza, y no debe confundirse con, una
ejecución científica real de la Etapa B (que sigue sin haberse realizado).

**Estado (2026-09-14):** el contrato de transferencia A→B (lectura estructural +
admisibilidad para una ejecución concreta) sigue implementado e integrado en la
escritura de artefactos de la Etapa A, verificado con pruebas exclusivamente
sintéticas -- ver la sección "Contrato de transferencia A→B" más abajo. Los tres
baselines del protocolo (sección 13) y el runner completo de la Etapa B (reentrenamiento
del candidato congelado, evaluación única sobre 2023, bootstrap pareado y veredicto)
quedan implementados e integrados en este cierre, reutilizando la Decisión 1 del
documento de decisiones pendientes -- adoptada para este encargo, ver
`docs/research/controlled-daily-v4-stage-b-c-decisiones-pendientes.md` y la nota de
adopción al pie de esta sección -- y verificados exclusivamente con pruebas
sintéticas (`tests/test_controlled_daily_v4_baselines.py`,
`tests/test_controlled_daily_v4_stage_b_runner.py`,
`tests/test_controlled_daily_v4_stage_b_integration.py`, más las extensiones de
`tests/test_controlled_daily_v4_cli.py`). La CLI habilita `--stage B` con
`--producer-dir` explícito y sigue rechazando `--stage C`. Ninguna ejecución
científica real de la Etapa B se realizó en este cierre; el runner de la Etapa C
(Decisiones 2 y 3, ambas bloqueadas) sigue sin implementar. Las tareas marcadas
**[BLOQUEADA]** no deben iniciarse hasta que exista una decisión explícita
registrada en `docs/research/controlled-daily-v4-stage-b-c-decisiones-pendientes.md`.

**Nota de adopción de la Decisión 1 (2026-09-14):** para este encargo se adopta la
propuesta técnica ya descrita en la Decisión 1 de ese documento -- las predicciones
evaluables de 2023 constituyen un único segmento temporal continuo para
`bootstrap.paired_bootstrap_delta` (`segment_id` constante,
`stage_b_runner.STAGE_B_BOOTSTRAP_SEGMENT_ID`), moving-block bootstrap pareado no
circular, bloques de 30 días, 5000 réplicas normativas, semilla `20250109`, los
mismos índices remuestreados aplicados a candidato, persistencia y etiquetas, sin
que ningún bloque cruce discontinuidades ni incorpore observaciones externas al
período autorizado. Esta adopción es una decisión de implementación de este
encargo, no una aprobación académica externa: se registra aquí exactamente como tal.

## Contrato de transferencia A→B

Dividido en dos partes que no deben mezclarse en una sola función: lectura/validación **estructural** (¿el artefacto se puede interpretar?) y validación de **admisibilidad** (¿este candidato puede usarse en esta ejecución concreta?). Ver `specs/experiment-runner/spec.md` de este *change* para el detalle de ambos requirements.

### Estructural (sin bloqueo — no depende de ninguna decisión abierta)

- [x] Agregar `input_mode`, `scientific_run`, `schema_version` propio y referencia a `dataset_fingerprint` dentro de `frozen_config.json` (extensión de `artifacts.py`; `TRANSFER_CONTRACT_SCHEMA_VERSION`, separada de `ARTIFACT_SCHEMA_VERSION` y de `DATASET_FINGERPRINT_FORMAT_VERSION`).
- [x] Agregar `depth_role` (`primary_selection` | `sensitivity_only_no_selection_effect`) **únicamente en `frozen_config.json`** (propuesta por defecto de la Decisión 4; `config.depth_role_for_column`), derivado de `--depth`. No duplicado en `selection_decision.json` -- decisión explícita de este *change*, registrada aquí y en `config.py`, no atribuida a una aprobación de terceros. Solo la eventual duplicación futura con verificación cruzada sigue abierta (Decisión 4).
- [x] Implementar la función de lectura/round-trip de `frozen_config.json` (`transfer_contract.load_frozen_config_contract`): reconstrucción de familia, hiperparámetros, `P20_train` final, exponiendo `input_mode`/`scientific_run`/`depth_role` sin decidir todavía si permiten continuar. Rechazo explícito de `schema_version` no soportado (`TransferContractSchemaError`) y de campos/tipos/valores no finitos/incoherencias estructurales (`TransferContractValidationError`).
- [x] Validar además la coherencia interna del contrato: `depth_role` debe corresponder a `depth_column` (reutilizando `config.depth_role_for_column`), `input_mode=synthetic` es incoherente con `scientific_run=true`, `selected_family` debe ser una familia reconocida y coincidir con la familia del candidato serializado, `family` debe coincidir con `config.family` (y, en Soft Voting, con la clave del diccionario), y `config.params` debe declarar explícitamente los hiperparámetros requeridos de cada familia (incluido `weighting`, balanceo de clases) con tipo/restricción válidos -- corrección de revisión externa 2026-09-13 sobre la primera versión de este *change*, que aceptaba estas incoherencias.
- [x] Validar el `scope` autorizado de toda referencia de fingerprint (`dataset_fingerprint.FINGERPRINT_SCOPE_STAGE_A_ELIGIBLE_ROWS`) y, para Soft Voting, los pesos de COMBINACIÓN del ensamble (`soft_voting_combination_weights`, protocolo sección 7.5 -- concepto distinto de `weighting`): obligatorios y con el valor normativo exacto (1/3 por base, `config.SOFT_VOTING_COMBINATION_WEIGHT`) cuando hay Soft Voting, ausentes cuando no lo hay -- segunda corrección de revisión externa 2026-09-13, que encontró que el contrato nunca los persistía.
- [x] Tests sintéticos: round-trip de escritura→lectura (familia única y Soft Voting, incluidos los pesos de combinación); rechazo de un `schema_version` no reconocido; JSON inválido; campos requeridos ausentes; `depth_role` contradictorio (tanto valor inválido como no correspondiente a `depth_column`); métricas no finitas; incoherencia de modo (`synthetic`+`scientific_run=true`); `selected_family` inventado o no coincidente con el candidato; `params` vacíos o sin `weighting`; familia no coincidente con `config.family` o con la clave de Soft Voting; referencia de fingerprint del productor vacía/inválida o con `scope` no autorizado; pesos de combinación ausentes, inventados, o presentes sin Soft Voting; ausencia explícita de candidato preservada sin fabricar una configuración congelada (`tests/test_controlled_daily_v4_transfer_contract.py`).

### Admisibilidad para una ejecución concreta (sin bloqueo — ver Decisión 4 solo para la ubicación de `depth_role`)

- [x] Implementar la validación de admisibilidad en el punto de consumo de la Etapa B (`admissibility.check_stage_b_admissibility`): acepta candidatos sintéticos cuando la ejecución que consume también es sintética (sin tocar ningún artefacto de evidencia científico); rechaza candidatos con `scientific_run=false` o `input_mode` no científico, con integridad no verificable, o con compatibilidad de procedencia fallida cuando la ejecución es científica.
- [x] Implementar **dos** verificaciones de commit distintas y no sustituibles entre sí (corrección de revisión externa 2026-09-13: la primera versión de este *change* solo implementaba la primera, y describía incorrectamente que bastaba): **(a)** consistencia INTERNA del productor -- el commit embebido en `frozen_config.json` debe coincidir con el de `code_version.json` del mismo directorio (misma corrida); un desacuerdo, sin política de compatibilidad documentada, se rechaza como mezcla de artefactos de corridas distintas; **(b)** compatibilidad PRODUCTOR-CONSUMIDOR -- comparación real entre el commit del productor y el commit de la ejecución consumidora, recibido explícitamente en `consumer_code_identity` (nunca calculado aquí); si difieren sin política documentada, se rechaza por "compatibilidad no acreditada". Se agrega también `consumer_environment_issues`, recibido explícitamente, y se exige que la evidencia de entorno del productor no dependa de una bandera aislada (`constraints_identity` verificable y `packages` capturados con contenido real).
- [x] Implementar la verificación de compatibilidad de procedencia para B: igualdad exacta de huella entre el conjunto de entrenamiento de B (recibido explícitamente como parámetro, nunca calculado aquí) y el conjunto derivado de A (mismo período) -- ambas huellas, y la del archivo hermano `dataset_fingerprint.json`, se revalidan por forma y contenido antes de comparar (SHA-256 válido, `schema_version` reconocido, `scope` autorizado), y se exige igualdad en los CUATRO metadatos requeridos (`schema_version`/`sha256`/`n_rows`/`scope`), no solo en el hash -- corrección de revisión externa 2026-09-13: una versión anterior admitía `n_rows`/`scope` divergentes con el mismo `sha256`. La verificación equivalente para C (identidad de fuente/provenance + extensión de período) queda fuera de este *change* (no se implementa runner de C).
- [x] Implementar la revalidación de la evidencia de entorno del productor con los mecanismos ya existentes (`environment.capture_constraints_identity`, `environment.validate_environment`), en vez de confiar en que los diccionarios persistidos tengan contenido: formato real del SHA-256 de `constraints.txt`, coincidencia con el archivo de referencia real, y validación normativa completa del entorno capturado contra el manifiesto/`constraints.txt` -- corrección de revisión externa 2026-09-13: una versión anterior admitía `constraints_identity`/`packages` con contenido inventado.
- [x] Implementar el rechazo por `depth_role` en el punto de consumo de la Etapa B, siempre como error duro, verificado antes que cualquier otro chequeo (severidad y punto de verificación ya resueltos — ver Decisión 4).
- [x] Tests sintéticos: un candidato sintético es admisible para una ejecución de integración sintética (sin crear ningún artefacto de evidencia científico); un candidato sintético se rechaza para una ejecución científica; un candidato con `scientific_run=true` pero árbol de trabajo sucio se rechaza igual; un candidato de B con huella de entrenamiento distinta de la de A se rechaza; ausencia o forma inválida de huella (productor, archivo hermano, o consumidor) se rechaza explícitamente (nunca se asume admisible ni se compara `None==None`); consistencia interna productor-productor y compatibilidad productor-consumidor verificadas por separado (commit coincidente/divergente en cada eje); evidencia de entorno insuficiente (bandera aislada) se rechaza; un candidato con `depth_role=sensitivity_only_no_selection_effect` se rechaza siempre como error duro; ausencia de candidato bloquea; `input_mode` de consumidor desconocido se rechaza; varias regresiones vía escritura real → modificación de JSON → lectura real → admisibilidad, no solo estados construidos a mano (`tests/test_controlled_daily_v4_admissibility.py`).
- [x] Verificación equivalente de compatibilidad de procedencia para C (identidad de fuente/provenance + extensión de período según `STAGE_C_BOUNDS`): implementada en `cli.py::_run_stage_c`, DESPUÉS de la confirmación durable de apertura del holdout -- compara `era5_sha256`/`nasa_power_sha256` de la corrida de C contra los registrados por A en `producer_dir/provenance.json` (misma fuente), sin exigir igualdad de huella de dataset (esa exigencia es específica de B). La extensión de período es estática (`config.STAGE_C_TRAINING_BOUNDS`), no una comparación en tiempo de ejecución.

## Baselines del protocolo (sección 13)

- [x] Implementar `predict_majority_class_baseline` (aprende exclusivamente de las etiquetas del `train` autorizado — acceso legítimo y requerido, no una fuga), `predict_persistence_baseline_v4` (usa la humedad actual y `P20_train`; nunca consulta la humedad futura de la fila evaluada) y `predict_constant_stress_baseline` (predice `1` siempre, sin ajustar ningún parámetro), parametrizados por el `train` autorizado de cualquier etapa. Implementado en `src/experiment_runner/controlled_daily_v4/baselines.py`. Regla de desempate de clase mayoritaria: el protocolo no la documenta; se define y documenta explícitamente `MAJORITY_CLASS_TIE_BREAK = 0` (predice ausencia de estrés ante empate exacto, consistente con la convención de igualdad ya usada por `features.build_target`), adoptada antes de escribir cualquier test que la ejercite, no elegida según resultados.
- [x] Tests sintéticos: clase mayoritaria coincide con `argmax` del conteo de etiquetas de `train` (verificando que el acceso a esas etiquetas ocurre y es correcto, no prohibiéndolo); igualdad exacta con `P20_train` produce clase 0 en persistencia; ninguno de los tres baselines consulta `future_soil_moisture` de la fila evaluada; ninguno de los tres cambia su regla al variar las etiquetas del conjunto de evaluación (manteniendo fijas las de `train`). `tests/test_controlled_daily_v4_baselines.py`.

## Etapa B

- [x] Implementar el bootstrap de `ΔMCC_B` reutilizando `bootstrap.paired_bootstrap_delta` sobre las predicciones evaluables de 2023, según la Decisión 1 -- adoptada para este encargo (ver la nota de adopción arriba): un único segmento temporal continuo (`STAGE_B_BOOTSTRAP_SEGMENT_ID`).
- [x] Implementar el reentrenamiento del candidato congelado (leído vía el contrato de transferencia) con `target_timestamp ≤ 2022-12-31`, evaluación única sobre 2023. Implementado en `src/experiment_runner/controlled_daily_v4/stage_b_runner.py` (`build_stage_b_training_frame`, `build_stage_b_evaluation_frame`, `refit_frozen_candidate`, `run_stage_b`): reutiliza exactamente familia/hiperparámetros/pesos de combinación congelados por A (incluido Soft Voting vía `models.fit_candidate`/`SoftVotingSpec`), sin búsqueda de hiperparámetros ni selección alternativa; verifica la coherencia de `P20_train` recalculado contra el del contrato (`StageBTechnicalError` si difieren).
- [x] Implementar el veredicto `CANDIDATE_VALIDATED`/`CANDIDATE_NOT_VALIDATED` exacto (protocolo, sección 10): `MCC_candidato_2023 > 0` **y** límite inferior del intervalo pareado de `ΔMCC_B ≥ −0.05`; monoclase en el `train` reentrenado o en la evaluación de 2023 ⇒ `CANDIDATE_NOT_VALIDATED` explícito (`decide_stage_b_verdict`), nunca una aprobación ni un fallo técnico.
- [x] Reemplazar `holdout_status.json` (antes literal constante) por un estado derivado real del veredicto persistido (`artifacts.write_stage_b_artifacts`: `stage_b_executed=true`, `stage_b_verdict=<veredicto>`; `holdout_2024_2025_open` permanece `false` -- ningún mecanismo de este *change* lo puede abrir, la Etapa C sigue sin implementar).
- [x] Habilitar `--stage B` en la CLI, condicionado a la existencia de un `frozen_config.json` válido (`--producer-dir` explícito, `cli.py::_run_stage_b`): lectura estructural → admisibilidad de la ejecución concreta (antes de tocar cualquier dato de humedad) → reentrenamiento/evaluación → artefactos. `--stage A` sin cambios de comportamiento; `--stage C` se sigue rechazando (`config.require_enabled_stage`, `config.CLI_ENABLED_STAGES = (STAGE_A, STAGE_B)`; `config.require_stage_a`/`config.SUPPORTED_STAGES` se conservan sin alterar, con su propio test existente intacto).
- [x] Tests sintéticos: los 4 cuadrantes del veredicto incluidas sus igualdades límite; caso monoclase de entrenamiento y de evaluación; distinción explícita entre `CANDIDATE_NOT_VALIDATED` (resultado experimental) y `StageBTechnicalError` (fallo técnico, p. ej. inconsistencia de `P20_train`); centinela de que ningún dato de 2024–2025 se toca durante la Etapa B (invariancia del resultado completo, no solo del frame de evaluación); rechazo previo al entrenamiento de un contrato inadmisible con espía sobre `run_stage_b`; ausencia de tuning (espía sobre `tuning.select_best_config`); Soft Voting respetado (pesos de combinación exactos 1/3); bootstrap con un único segmento y verificación explícita de la configuración normativa completa (5000 réplicas) en un test dedicado; CLI de B operativa de punta a punta (A sintético → contrato → B → artefactos y decisión) y C todavía bloqueada tras A y B. `tests/test_controlled_daily_v4_stage_b_runner.py`, `tests/test_controlled_daily_v4_stage_b_integration.py`, extensiones de `tests/test_controlled_daily_v4_cli.py`.

## Etapa C

**Estado (2026-09-15, encargo "Etapa C y ledger de protección del holdout"):**
las Decisiones 2 y 3 fueron adoptadas como decisiones operativas de este
encargo (ver `docs/research/controlled-daily-v4-stage-b-c-decisiones-pendientes.md`,
nota de adopción al comienzo del documento) y todas las tareas antes
bloqueadas por ellas quedan implementadas e integradas en este cierre,
verificadas exclusivamente con pruebas sintéticas. Ninguna ejecución
científica real de la Etapa C, ni apertura real del holdout 2024-2025, se
realizó en este cierre.

- [x] Implementar el registro persistente (ledger) del holdout, keyado por la identidad estable del holdout (protocolo + sitio + profundidad + período — no por commit/candidato/`--output-dir`), con tres estados posibles por identidad de holdout (`AUSENTE`/`CONFIRMADA`/`INDETERMINADA`), inicialización explícita (estado inicial `AUSENTE`, nunca inferido de la ausencia del archivo) y un ledger sintético completamente separado del científico. Implementado en `src/experiment_runner/controlled_daily_v4/holdout_ledger.py` como SQLite (biblioteca estándar) en una ubicación explícita fuera de `--output-dir` -- no como archivo versionado en Git (ver la nota de adopción de la Decisión 2 para la justificación de este cambio respecto de la propuesta original).
- [x] Implementar la secuencia de apertura en el orden exacto de la Decisión 2, sin alterarlo: (1) verificar veredicto `CANDIDATE_VALIDATED` de B, autorización (`--authorized-by`) y precondiciones de entorno — sin tocar ningún dato de 2024–2025 (`admissibility.check_stage_c_admissibility`, cero I/O de CSV); (2) consultar el estado del ledger para esta identidad de holdout — proceder a reservar solo si es `AUSENTE`; rechazar de inmediato si es `CONFIRMADA` o `INDETERMINADA` (`holdout_ledger.reserve_holdout`); (3) confirmar la apertura con `fsync` explícito del archivo (y del directorio, cuando el mecanismo lo permite) antes de considerarla confirmada (`holdout_ledger.confirm_holdout_open`) -- mecanismo propio, no reutiliza `artifacts._atomic_write_text`; (4) recién entonces, `cli.py::_run_stage_c` valida provenance (hash completo de los CSV) y carga/evalúa datos de 2024–2025; (5) marca de finalización separada (`holdout_ledger.finalize_holdout`) al terminar de serializar los resultados.
- [x] Implementar el reentrenamiento con `target_timestamp ≤ 2023-12-31` y `P20_train` recalculado en ese rango (`stage_c_runner.py`: `build_stage_c_training_frame`, `refit_frozen_candidate_for_stage_c`) -- deliberadamente sin comparar ese `P20_train` contra `contract.final_p20_train` (esa exigencia es específica de B, que reentrena sobre el mismo período que A; C reentrena sobre un período extendido).
- [x] Implementar la evaluación única sobre `[2024-01-04, 2025-12-31]` (`stage_c_runner.run_stage_c`), condicionada a que los pasos 1-3 se hayan completado exitosamente y de forma durable (verificado por `cli.py::_run_stage_c`, que solo invoca `run_stage_c` después de `confirm_holdout_open`).
- [x] Definir la política de `--overwrite` para `--stage C`: prohibición incondicional (Decisión 3, adoptada) -- `cli.py` la rechaza antes de cualquier otra verificación, y `artifacts.write_stage_c_artifacts` ni siquiera expone el parámetro.
- [x] Habilitar `--stage C` en la CLI (`config.CLI_ENABLED_STAGES` incluye `STAGE_C`; `cli.py::_run_stage_c`), con las restricciones de las decisiones 2 y 3.
- [x] Documentar y verificar con test sintético los dos estados terminales excepcionales, ambos sin reintento automático y sin borrado/reinicialización silenciosa del registro: (a) apertura confirmada (paso 3) sin marca de finalización (paso 5) -- `test_reservation_rejects_second_reservation_when_confirmed`, `test_two_concurrent_processes_only_one_wins_the_reservation`; (b) registro `INDETERMINADA` (interrupción durante los pasos 2-3) -- `test_indeterminate_reservation_blocks_access_and_is_not_reset`, `test_confirm_with_wrong_attempt_id_leaves_indeterminate`. Ambos requieren intervención humana fuera de la CLI (documentado en el módulo y en el documento de decisiones); ningún comando automático de la CLI borra, resetea o libera una reserva.
- [x] Tests sintéticos (ver `tests/test_controlled_daily_v4_holdout_ledger.py`, `tests/test_controlled_daily_v4_stage_c_runner.py`, `tests/test_controlled_daily_v4_stage_c_admissibility.py`, `tests/test_controlled_daily_v4_stage_c_integration.py`): rechazo si no hay `CANDIDATE_VALIDATED` previo, incluida la revalidación de coherencia decisión/métricas/bootstrap (no un `decision.json` aislado); la reserva rechaza una segunda apertura con la misma identidad de holdout cuando el estado es `CONFIRMADA`, aunque cambien el intento (equivalente a commit/candidato/`--output-dir` en la CLI real); la reserva también rechaza cuando el estado es `INDETERMINADA` (simulado forzando una interrupción entre reserva y confirmación), sin borrar ni reinicializar ese registro; un fallo simulado *antes* de cualquier intento de reserva (estado `AUSENTE`) deja el holdout intacto; un fallo simulado *después* de la confirmación (durante la evaluación o la escritura) deja el holdout marcado `CONFIRMADA` de forma permanente, sin reintento automático; una invocación posterior con la marca de finalización ya presente recupera el resultado existente en vez de reevaluar (solo lectura, sin reentrenar ni acceder al holdout crudo); el ledger sintético de los tests de integración nunca modifica ni consulta ningún ledger científico (no existe ninguno real en este repositorio); dos procesos reales del sistema operativo (no mocks) compitiendo por la misma reserva -- solo uno la obtiene; espías sobre `ingestion.load_era5_hourly_raw` confirman que ningún CSV se lee antes de la reserva/autorización correspondiente en los casos de rechazo previo.

## Integración y documentación

- [x] Test de integración sintética de punta a punta A→B (con fixtures sintéticas, sin CSV reales): artefactos de A → contrato → admisibilidad → B → artefactos y decisión, con CLI operativa de punta a punta (`tests/test_controlled_daily_v4_stage_b_integration.py`).
- [x] **Resuelto (2026-09-15):** cadena completa A→B→C mediante los contratos reales, con `CANDIDATE_NOT_VALIDATED` en B deteniendo el acceso a 2024–2025 y `CANDIDATE_VALIDATED` habilitando la apertura del ledger y la evaluación completa de C -- `tests/test_controlled_daily_v4_stage_c_integration.py` (`test_end_to_end_synthetic_a_to_b_to_c_full_flow`, `test_stage_c_rejects_when_b_not_validated_without_touching_holdout`, y las pruebas de recuperación/rechazo de doble apertura).
- [ ] Completar los campos `stage_b_result`/`stage_c_result` del manifiesto de provenance (hoy `PENDING_BEFORE_EXECUTION`) únicamente cuando exista una ejecución real que los sustente — no antes.

## Revisión dirigida sobre PR #194 (2026-09-15)

Cinco hallazgos reproducidos y corregidos sobre el mismo PR, con regresión
sintética previa a cada corrección (no se reabre la Decisión 4 de
`depth_role`):

- [x] **Admisibilidad B→C insuficiente:** `check_stage_c_admissibility` aceptaba booleanos como métrica (`isinstance(True, (int, float))` es verdadero en Python), MCC/intervalo no finitos, un intervalo bootstrap invertido, `diagnostics.replicas_valid=0`, y una corrida de B con `input_mode='synthetic'` pero `scientific_run=True`. Corregido con `_validate_bounded_metric` (rechaza booleano/no finito/fuera de `[-1, 1]`), validación de `interval_lower <= interval_upper`, contabilidad de réplicas (`replicas_valid > 0`, `valid + discarded == requested`), y coherencia explícita `resolved_config.input_mode`/`scientific_run` en ambos sentidos (sintético y científico). Fixtures de `tests/test_controlled_daily_v4_stage_c_admissibility.py` reconstruidas con evidencia completa (antes omitían `interval_upper`/`diagnostics`/`input_mode`); 6 regresiones nuevas, incluido un spy que verifica cero reservas/cero acceso al holdout en cada caso rechazado.
- [x] **Recuperación con falso éxito:** un ledger `CONFIRMADA`+finalizado apuntando a un directorio inexistente devolvía `exit 0`. Corregido con `artifacts.verify_stage_c_recovery` + `integrity_manifest.json` (sha256 de cada artefacto, `holdout_identity_key`/`attempt_id`, `result_reference` resuelto/absoluto), escrito como último artefacto de `write_stage_c_artifacts`; `cli.py` verifica antes de declarar recuperación satisfactoria y nunca reentrena. `tests/test_controlled_daily_v4_stage_c_recovery.py` (7 tests): recuperación válida, directorio ausente, artefacto faltante, contenido alterado, intento distinto, manifiesto faltante.
- [x] **Salida ya ocupada llegaba al acceso del holdout:** la comprobación de ocupación de `--output-dir` solo ocurría dentro de `write_stage_c_artifacts`, después de reservar/confirmar/evaluar. Corregido moviendo `artifacts.check_output_directory_not_occupied` (nueva función, sin efecto de creación de directorio) al paso 1 de `cli.py::_run_stage_c`, antes de la admisibilidad y la reserva; se agregó también el rechazo previo de `--authorized-by` vacío y de una invocación científica con semilla/réplicas no normativas. 3 regresiones nuevas en `tests/test_controlled_daily_v4_stage_c_integration.py` verifican cero reservas y ledger en `AUSENTE`.
- [x] **Validación de esquema del ledger incompleta:** cambiar `ledger_meta.schema_version` a un valor desconocido no se validaba en absoluto -- `read_holdout_state` devolvía el estado real (no `AUSENTE`, como se creía, sino simplemente sin verificar el esquema) y `reserve_holdout` reservaba igual. Corregido centralizando la validación en `_validate_ledger_meta` (esquema + `mode` coherente), aplicada en lectura (→ `INDETERMINADA`), reserva/confirmación/finalización (→ rechazo explícito, `HoldoutLedgerSchemaError`); `confirm_holdout_open`/`finalize_holdout` ahora verifican explícitamente que el archivo exista (antes dejaban que `sqlite3.connect` creara uno vacío implícito); `finalize_holdout` exige el `attempt_id` propietario de la reserva (`HoldoutFinalizationOwnershipError`) y rechaza una segunda finalización (`HoldoutAlreadyFinalizedError`) en vez de reemplazar la referencia en silencio. 8 regresiones nuevas en `tests/test_controlled_daily_v4_holdout_ledger.py`, conservando la prueba de dos procesos reales del sistema operativo.
- [x] **Trazabilidad del conjunto de C incompleta:** el entrenamiento extendido de C (hasta 2023) se huellaba con `scope='stage_a_eligible_rows_only'` (el de A/B, que describe un período distinto) y `predictions_2024_2025.csv` no incluía `target_timestamp`. Corregido con `FINGERPRINT_SCOPE_STAGE_C_EXTENDED_TRAINING` (nuevo `scope` explícito, `compute_dataset_fingerprint` ahora acepta `scope` como parámetro, default sin cambios para A/B) y `StageCResult.target_timestamps` persistido en `predictions_2024_2025.csv` (esquema de artefactos de C `controlled_daily_v4_stage_c.v2`). 2 regresiones nuevas en `tests/test_controlled_daily_v4_stage_c_runner.py`; sin regresiones en A/B (mismo `scope` por defecto).
- [x] Actualizar `openspec/specs/experiment-runner/spec.md` (canónico) para reflejar las capacidades de B (y, con el mismo criterio, de A) una vez que el código de este *change* esté integrado y verificado con pruebas sintéticas — el mismo criterio de "implementado" ya usado para otras capacidades de HU7 en este documento (código integrado/verificado + verificación sintética), **no** condicionado a que exista una ejecución científica real. **Resuelto (2026-09-15, encargo explícito sobre PR #193):** esta tarea documentaba una contradicción, no resuelta en su momento -- la tarea equivalente de `implement-controlled-daily-v4-stage-a/tasks.md` (línea 19 original, conservada allí como registro histórico) dejaba la actualización del spec canónico "pendiente de una corrida real y de una decisión explícita de cuándo el spec canónico debe actualizarse", por lo que actualizar el spec solo para B habría sido inconsistente con ese precedente. El encargo del 2026-09-15 resolvió esa decisión explícitamente para A y B a la vez (no se inventa aquí): documentar una capacidad implementada e integrada (A) o implementada y verificada sintéticamente en un PR todavía abierto (B) no depende de una ejecución científica real. El spec canónico incorpora la nueva sección "Protocolo controlled_daily_v4_external_pergamino (Etapas A y B)", que distingue explícitamente: A implementada e integrada; B implementada y verificada sintéticamente en el PR #193, con su integración a `main` pendiente mientras ese PR siga abierto; las ejecuciones científicas reales de A y B, sus resultados, y la Etapa C con su ledger, todos pendientes/sin implementar por separado (ver la tarea anterior, sobre `stage_b_result`/`stage_c_result`, y la sección "Etapa C" de este archivo) -- ninguno de ellos se documenta como obtenido ni implementado por esta actualización.

## Segunda revisión dirigida sobre PR #194 (2026-09-15)

Cinco reproducciones adicionales confirmadas sobre el mismo PR (commit
auditado `b3bf060`, posterior al cierre de los cinco hallazgos de la primera
revisión), corregidas con regresión sintética previa a cada corrección
(`depth_role` sigue sin reabrirse):

- [x] **Admisibilidad B→C todavía incompleta:** se aceptaban `diagnostics={"replicas_valid": 1}` sin los demás contadores, `valid=5, requested=1, discarded=-4` (la contabilidad `valid+discarded==requested` daba casualmente igual con un descarte negativo), una corrida científica con bootstrap reducido/no normativo, y evidencia de B sin `training_dataset_fingerprint.json`. Corregido exigiendo los tres contadores presentes como enteros no booleanos no negativos (`requested>0`, `valid>0`, `valid+discarded==requested`, ningún contador ausente tratado como si fuera `0`), verificando `diagnostics.normative is True` para el camino científico (leído tal cual lo persistió B, nunca recalculado aquí), y cargando/validando estructuralmente `training_dataset_fingerprint.json` de B (sin exigirle igualdad con el fingerprint EXTENDIDO de C, que describe un período distinto). Fixtures de `tests/test_controlled_daily_v4_stage_c_admissibility.py` actualizadas para escribir por defecto un `training_dataset_fingerprint.json` válido y `diagnostics.normative=True`.
- [x] **Dominio de ΔMCC incorrecto (bug propio de la primera corrección):** `interval_lower`/`interval_upper` de `bootstrap.json` reportan una DIFERENCIA de dos MCC (dominio `[-2, 2]`), pero se validaban contra el dominio de un MCC aislado (`[-1, 1]`), rechazando incorrectamente intervalos válidos como `[1.1, 1.3]`. Corregido con `_DELTA_MCC_LOWER_DOMAIN`/`_DELTA_MCC_UPPER_DOMAIN` (`[-2, 2]`) aplicado exclusivamente a `interval_lower`/`interval_upper`, sin tocar el umbral de aprobación de B (`interval_lower >= -0.05`) ni el dominio de `mcc_candidate.value` (que sigue en `[-1, 1]`).
- [x] **Recuperación verificable todavía incompleta:** un `integrity_manifest.json` con `schema_version` desconocido, o con `files` conteniendo únicamente un archivo ajeno (`only.txt`), pasaba la verificación. Corregido: `verify_stage_c_recovery` exige `schema_version == STAGE_C_ARTIFACT_SCHEMA_VERSION` (ahora `.v3`), que `files` cubra `REQUIRED_STAGE_C_ARTIFACT_NAMES` completo, que cada ruta esté confinada a `output_dir` (`_is_path_confined`: rechaza rutas absolutas, `..`, o resoluciones/enlaces simbólicos fuera del directorio) y coherencia estructural mínima (cada `.json` declarado parsea, y `schema_version.json` coincide con el del manifiesto). Además, la recuperación del PROPIO directorio ya finalizado ahora funciona aunque ese directorio esté "ocupado": `cli.py::_run_stage_c` lee el estado del ledger (y resuelve la rama de recuperación) ANTES de aplicar `check_output_directory_not_occupied` -- antes, ese chequeo corría primero y rechazaba erróneamente una recuperación que reutilizara el mismo `--output-dir` de la corrida ya finalizada.
- [x] **Coherencia del registro del ledger todavía incompleta:** un registro con `state='AUSENTE'` pero `confirmed_at`/`finalized_at`/`finalized_result_reference` ya poblados pasaba como AUSENTE real y permitía reservar de nuevo. Corregido con `_row_coherence_issue` (verifica que las columnas de la fila correspondan a alguna transición válida AUSENTE→INDETERMINADA→CONFIRMADA, incluyendo finalización parcial), aplicado en las cuatro operaciones (`read_holdout_state` degrada a `INDETERMINADA`; `reserve_holdout`/`confirm_holdout_open`/`finalize_holdout` rechazan con `HoldoutRegistryCoherenceError`, nueva excepción, sin reparar ni resetear ninguna columna).
- [x] **Identidad de los datos de C todavía incompleta:** no existía una huella separada del conjunto EFECTIVAMENTE evaluado del holdout (solo la del entrenamiento extendido). Corregido con `FINGERPRINT_SCOPE_STAGE_C_EVALUATION` (nuevo `scope`) y `StageCResult.evaluation_dataset_fingerprint`, calculado dentro de `run_stage_c` (por lo tanto, solo después de la apertura autorizada -- precondición ya documentada del módulo), persistido como `evaluation_dataset_fingerprint.json` e incluido en `integrity_manifest.json`/`REQUIRED_STAGE_C_ARTIFACT_NAMES` (esquema de artefactos de C `controlled_daily_v4_stage_c.v3`). Los fingerprints históricos de A/B no se tocan.

Regresiones nuevas: 1 en `tests/test_controlled_daily_v4_stage_c_admissibility.py` (fixtures reconstruidas, ejercitadas por los tests existentes de diagnostics/normative/fingerprint faltante), 6 en `tests/test_controlled_daily_v4_stage_c_recovery.py` (schema desconocido, conjunto incompleto, escape por `..`, escape por ruta absoluta, coherencia estructural de `schema_version.json`), 4 en `tests/test_controlled_daily_v4_holdout_ledger.py` (AUSENTE/INDETERMINADA/CONFIRMADA incoherentes, finalización parcial), 1 en `tests/test_controlled_daily_v4_stage_c_integration.py` (recuperación del propio directorio ocupado). Suite completa `tests/test_controlled_daily_v4_*.py` sin regresiones (ver PR #194 para el conteo exacto del commit final).
